# AGENT.md — Quiz HTML (PDF Viewer + Answer Sheet)

이 문서는 Codex(로컬 에이전트)에게 “현재 폴더의 퀴즈 웹앱”을 유지·확장하기 위한 작업 지시 프롬프트입니다.

## 목표
- `quiz.html` 하나로 다음을 제공한다.
  - 왼쪽: 시험 **문제 PDF** 뷰어(iframe)
  - 오른쪽: 1~100번 **답안지** (1~4 선택)
  - **다시풀기(리셋)** 버튼
  - **자동채점** 옵션(선택 즉시 정답/오답 표시)
  - **전체 채점** 버튼
  - **오답/미응답 필터** + 첫 항목 점프
  - **로컬 저장(localStorage)**: 새로고침/재접속해도 답안 유지
  - **섹션별 점수**: 1~25 / 26~50 / 51~75 / 76~100
  - **오답노트 자동 생성 + 복사 버튼**
  - **오답만 다시풀기(2회전 모드)**: 오답 문항만 표시, 나머지 잠금
    - 2회전 통계(대상 문항/2회전 정답/2회전 미응답)
    - 2회전 모드에서 **오답 문항만 초기화** 버튼
  - **키보드 단축키**
    - `1~4`: 현재 포커스 문항 답 선택
    - `←/→`: 현재 보이는 목록 기준 문항 이동
    - `G`: 전체 채점 + 오답노트 생성
    - `R`: 전체 리셋
    - `J`: 현재 필터(또는 2회전) 기준 첫 문항으로 점프
    - `Esc`: 2회전 모드 종료

## 파일 구성(로컬)
- `quiz.html`  (앱 본체)
- `2025년 제22회 농산물품질관리사 1차 시험문제지.pdf`  (문제)
- `최종정답.pdf` (정답표 참고용)

> PDF 파일명이 다르면 `quiz.html` 상단의 `QUESTION_PDF_URL`, `ANSWER_PDF_URL`만 수정.

## 실행 방법(권장)
브라우저에서 `file://`로 열면 PDF iframe이 막히는 경우가 있어, 로컬 서버 실행을 권장:

```bash
# macOS / Linux / Windows(파워셸도 유사)
python -m http.server 8000
# 이후 브라우저에서
# http://localhost:8000/quiz.html
```

## 정답키(ANSWER_KEY) 관리
- `quiz.html`의 `ANSWER_KEY`는 1-based 배열이며, 인덱스 1~100을 사용한다.
- 기본 UI는 1~4 선택지만 제공한다.
- 만약 실제 시험지에 5번 선지가 존재한다면(예: 일부 과목/유형) UI를 1~5로 확장해야 한다.
  - 확장 시: 선택지 생성 루프 `for(let c=1; c<=4; c++)`를 `<=5`로 변경하고,
    스타일 grid 컬럼도 5칸으로 조정(또는 자동).

## 유지보수/확장 시 규칙
- 외부 라이브러리 없이 순수 HTML/CSS/JS 유지
- 데이터 저장은 `localStorage`만 사용(서버 의존 X)
- 기능 추가 시에도 “단일 파일(quiz.html)” 원칙 유지
- UX: 버튼/토글은 상단 또는 패널 내부에 유지, 작은 화면에서도 동작해야 함
- 성능: 100문항 렌더링은 유지하되, DOM 반복을 최소화(필요 시 documentFragment)

## 향후 개선 아이디어(선택)
- 과목별 접기/펼치기(섹션 UI)
- 오답만 재시험 2회전 → 3회전(스페이스드 반복)
- 오답노트에 메모(사용자 입력) 저장 기능
- “실전 모드” (정답표시/자동채점 강제 OFF)

## 작업 기록 규칙
- 지금부터 수정 확정 사항 또는 주요 변경 사항은 반드시 `AGENT.md`에 기록한다.
- 기록 타이틀 형식은 아래를 사용한다.
  - `주요 내용 + yyyy-MM-dd hh:mm:ss`
- 시간은 로컬 현재 시각 기준 24시간 형식으로 기록한다.

## 변경 로그
### 주요 내용 + 2026-06-03 17:00:33
- 확정 유지보수 원칙 추가:
  - PDF 문항 표시, 채점 표시, 앵커/문항영역 보정은 공통성을 유지해야 하며 특정 사례 수정이 현재 정상 동작 중인 회차·문항·모드에 영향을 주지 않도록 조건을 좁혀 적용한다.
  - 한문제/전체보기, 1단/2단, 앵커/문항영역 표시 변경은 기존 정상 좌표·스케일·크롭 경로를 먼저 보존하고, 필요한 경우 검증 가능한 최소 보정만 추가한다.

### 주요 내용 + 2026-02-28 15:27:41
- 확정 규칙 추가: 이후 모든 확정/주요 변경은 `AGENT.md`에 지정 포맷 타이틀로 기록.

### 주요 내용 + 2026-02-28 15:30:35
- 정답 PDF 동적 파서 보강:
  - `textContent.items`를 좌표 기반 줄 텍스트로 재구성해 추가 파싱
  - 2행 테이블형(윗줄 문항번호 / 아랫줄 정답번호) 패턴 지원
  - 전체 텍스트 스캔 정규식 확장(`12) 3`, `12-3`, `12:3` 등)
  - 페이지당 다중 소스(원본개행/평탄화/행재구성) 교차 투표로 정답 후보 안정화

### 주요 내용 + 2026-02-28 15:34:33
- 정답 추출 누락 대응 2차 보강:
  - 숫자 스트림 전용 파서 추가(`문항-정답` 교차 수열 직접 해석)
  - `textContent.items`의 숫자 토큰을 순차 파싱해 잡음(날짜/페이지) 사이에서도 유효 페어 복원
  - 숫자 스트림 파싱 결과에 가중치(+2)를 부여해 실제 정답 패턴 우선 반영
  - 충돌 해소 임계값 완화(상위 득표수 기준)로 `null` 누락 비율 감소

### 주요 내용 + 2026-02-28 15:40:43
- 정답 로딩 정책 전환:
  - `quiz.html`에서 `questionNo` 기반 `json/answers/<questionNo>.json`만 정답 로드
  - JSON이 없으면 정답키를 로드하지 않음(`정답키: 미확정`)
  - 회차 링크 생성 시 `questionNo` 쿼리 파라미터를 함께 전달
- 정답 JSON 생성 스크립트 추가:
  - `scripts/generate-answer-json.mjs` 작성
  - `json/question.json`을 순회해 회차별 정답 JSON 생성
  - 표형 행 파싱 + 숫자 스트림 보강 파싱 적용

### 주요 내용 + 2026-02-28 15:41:46
- 정답 JSON 저장/로딩 경로 변경:
  - 생성 스크립트가 `answerPdf`와 동일 경로에 `"정답파일명".json` 생성하도록 수정
  - `quiz.html`은 `ANSWER_PDF_URL`을 `.json`으로 치환한 경로만 로드
  - 즉, 정답 JSON은 `pdf/.../최종정답.json` 형태로 관리

### 주요 내용 + 2026-02-28 15:43:22
- 정답 JSON 포맷 조정:
  - `"answers"` 배열을 5개 값 단위로 줄바꿈해 출력하도록 생성 스크립트 수정
  - 가독성 향상을 위한 배열 직렬화 포맷(`null/숫자`) 적용

### 주요 내용 + 2026-02-28 15:44:22
- 미사용 디렉토리 정리:
  - `json/answers` 디렉토리 삭제(현행 정답 로딩 경로와 무관)

### 주요 내용 + 2026-02-28 15:45:45
- 정답 JSON 전체 재생성:
  - 모든 회차의 `"answers"` 배열을 5개 값 단위 줄바꿈 포맷으로 재출력

### 주요 내용 + 2026-02-28 15:46:52
- 정답 JSON 생성 스크립트 동작 변경:
  - 동일 경로에 JSON 파일이 이미 있으면 기본 동작에서 재생성하지 않고 `SKIP`
  - 강제 재생성은 `--force` 옵션으로만 수행

### 주요 내용 + 2026-02-28 15:51:04
- 정답 JSON 인덱스 포맷 변경:
  - `"answers"` 배열을 0번 패딩 없이 100개(1번~100번) 값으로 저장
  - `quiz.html` 로더는 100개(0-based)·101개(1-based) 포맷 모두 호환 처리

### 주요 내용 + 2026-02-28 15:53:06
- 정답 JSON 전체 강제 재생성 수행:
  - `node ./scripts/generate-answer-json.mjs --force` 실행
  - 인자 파싱 버그 수정(`--force`가 회차번호로 오인되던 문제 해결)

### 주요 내용 + 2026-02-28 16:03:53
- 정답키 상태 표시 조건 조정:
  - `정답키: JSON 부분(n)` 표시는 로컬 실행(`file://`, `localhost`, `127.0.0.1`, `::1`)에서만 노출
  - 비로컬 환경에서는 카운트 없이 `정답키: JSON`으로 표시

### 주요 내용 + 2026-02-28 16:06:43
- 정답 추출 전처리 강화:
  - 정답 파싱 시 각 줄에서 숫자/공백 이외 문자 제거 후(`41 1` 형태) 페어 추출
  - 전체 숫자 스트림 fallback도 동일 규칙(비숫자 제거)으로 통일

### 주요 내용 + 2026-02-28 16:22:14
- 수동 확정값 반영:
  - 2024/2023/2022 정답 JSON을 PDF 직접 판독값으로 덮어씀
  - `answers` 100개, 값 범위 1~5, 5개 단위 개행 포맷 검증 완료

### 주요 내용 + 2026-02-28 16:37:06
- GPT 검증 워크플로우 스크립트 추가:
  - `scripts/compare-answer-json.mjs`: 기존 JSON과 GPT 배열 비교(불일치 리포트)
  - `scripts/apply-gpt-answers.mjs`: GPT 배열을 JSON에 반영(100개/1~5 검증 + 5개 개행 포맷 저장)

### 주요 내용 + 2026-02-28 16:48:45
- GPT 배열 검증 엄격화:
  - 비교/반영 스크립트에서 배열 길이를 엄격 검증(정확히 100 또는 `101+첫값 null`만 허용)
  - 100개 초과/미만 배열 입력 시 즉시 에러로 중단

### 주요 내용 + 2026-02-28 17:24:04
- 상단 안내 문구 변경:
  - `오답노트·단축키(1~4/←→/G/R)` → `단축키( 문제이동 : ←→ 답 : 1~4 )`

### 주요 내용 + 2026-02-28 17:25:27
- 문제 PDF 헤더 배치 변경:
  - `왼쪽 문제 / 오른쪽 답 체크 · 페이지이동 ...` 문구 영역 제거
  - `현재 n번` 오른쪽에 `정답키: ...` 배치
  - `정답키` 옆에 `모드` 버튼 배치

### 주요 내용 + 2026-02-28 17:27:28
- 보기모드 상태 표시 분리:
  - `정답키` 옆에는 버튼 대신 상태 텍스트(`모드: 전체/한 문제`) 표시
  - `↕/↔` 보기모드 전환 버튼은 우측 컨트롤 영역으로 복귀

### 주요 내용 + 2026-02-28 17:30:20
- 저품질 전체보기 말미 문항 스크롤 보정:
  - 전체보기에서 탐지 ratio가 과도하게 큰 경우(하단 푸터 과점프) 페이지맵 ratio로 보정
  - 스크롤 ratio 하한/상한 클램프 적용(0.04~0.88)으로 98~100번 하단 오표시 완화

### 주요 내용 + 2026-02-28 17:31:22
- 보기모드 상태 라벨/색상 개선:
  - 상태 문구 `모드: 한문제/모드: 전체`에서 `한 문제/전체`로 단순화
  - 모드 상태 배경색 추가(전체=중립, 한 문제=강조) 및 다크 테마 색상 대응

### 주요 내용 + 2026-02-28 17:31:57
- 문제 PDF 헤더 정리:
  - 타이틀 바의 `문제 PDF` 표시 행 제거

### 주요 내용 + 2026-02-28 17:33:30
- 전체보기 앵커 계산 보강:
  - 전체보기 스크롤 타깃에 한문제 보기의 `getQuestionCropRange()` 시작점 로직을 재사용
  - 저품질 문서 말미 문항(98~100) 하단 과점프/오표시 완화

### 주요 내용 + 2026-02-28 17:42:49
- 정답키 배지 노출 조건 제한:
  - `정답키` 배지는 `localhost` 또는 `127.0.0.1`에서만 표시
  - 그 외 호스트에서는 배지를 숨기고 업데이트 로직도 건너뜀

### 주요 내용 + 2026-02-28 17:49:01
- 배포 경로 인코딩 호환 보강:
  - PDF/JSON 로드 시 `%` 인코딩 경로를 `decodeURIComponent` 후 NFC/NFD 후보로 재시도
  - 인코딩 URL/원문 URL 모두 fetch 후보로 순차 탐색하도록 경로 해석 로직 개선

### 주요 내용 + 2026-03-01 02:56:38
- 모바일 사용성/안정성 보강:
  - 모바일 도크에 답안 선택 `1~4` 직접 노출(기존 1,2만 표시 문제 해결)
  - 모바일 `이전/다음` 버튼 라벨을 `<` / `>`로 변경
  - 모바일에서 전체보기 비정상 회피를 위해 `한 문제 보기` 고정 및 최소 확대(zoom 1.1) 적용
  - 모바일 뷰어 최소 높이/하단 도크 여백 조정으로 가독성 개선

### 주요 내용 + 2026-03-01 03:00:01
- 모바일 보기모드 전환 복구:
  - 모바일에서 `한 문제` 고정하던 제한 로직 제거
  - 모바일에서도 `↕/↔` 보기모드 토글 가능하도록 변경

### 주요 내용 + 2026-03-01 03:16:17
- 모바일 가로(landscape) 레이아웃 최적화:
  - 가로 모드에서 문제/답안 2컬럼 동시 표시로 전환
  - 하단 모바일 도크/오버레이 숨김으로 화면 혼잡도 감소
  - 헤더/컨트롤 버튼 컴팩트화, 불필요 버튼(`문제 PDF 새창`, `정답 PDF 새창`) 숨김
  - 뷰어/시트 높이 재조정으로 가독성 개선

### 주요 내용 + 2026-03-01 03:21:03

### 주요 내용 + 2026-03-04 00:44:09
- 한문제 보기 크롭 경계 안정화(다음 문항 누수 재발 방지):
  - `renderPdfPage()`에서 `q+1` 실패 시에도 같은 페이지의 `q+2...` 시작점 후보를 수집해 첫 다음 문항 경계로 사용
  - 경계 후보 소스를 `질문 시작 앵커 / questionRatioMap / pageMap / locateQuestionInPdf`로 확장
  - 선택지 하단 보정(`minEndByChoice`)이 경계를 무시하지 않도록 경계 상한으로 캡 적용
  - 저신뢰 보정/최소 높이 보정 이후에도 마지막에 경계 상한을 다시 강제 적용해 `19번 아래 20번 노출` 같은 누수 차단
  - 선택지 탐지 최소 개수를 `>=3`에서 `>=2`로 완화해 `1번에서 2번까지만 보이는` 과도 컷 케이스 완화

### 주요 내용 + 2026-03-04 00:55:10
- 한문제 보기 누수 재보강(5번→6번 혼입 케이스):
  - `renderPdfPage()`에서 `mappedAnyNextSamePage`(같은 페이지의 첫 다음 문항 비율) 계산 추가
  - 경계 후보 필터에서 `minEndByChoice` 기준 배제 조건 제거(경계 누락 방지)
  - `q+1` 경계 실패 시에도 `mappedAnyNextSamePage`를 하드 상한으로 사용해 다음 문항 노출 차단

### 주요 내용 + 2026-03-04 01:00:40
- 한문제 보기 단일문항 경계 최종 보강:
  - `getQuestionCropRange()` 반환값에 `nextBoundary` 추가(경계 정보 전달)
  - `renderPdfPage()`에서 `range.nextBoundary`를 하드 상한 후보로 사용
  - 경계 탐지 실패 시 `선지 하단 + 여유` 기반 `maxEndByChoice` 상한을 적용해 다음 문항 제목 혼입 방지
  - 하단 패딩 경계 계산이 `hardEndCapRatio`를 우선 사용하도록 조정

### 주요 내용 + 2026-03-04 01:12:56
- 한문제 보기 7→8 경계 누락 보정:
  - `renderPdfPage()`의 다음 문항 후보 필터 최소 간격(`minGap`)을 `0.06`으로 완화
  - 정상적인 인접 문항 시작점이 과도 필터로 탈락하던 문제를 줄여 단일문항 누수(다음 문항 제목 노출) 방지

### 주요 내용 + 2026-03-04 01:30:18
- 한문제 보기 경계 로직 전면 일원화(전수 안정화 목적):
  - `getNextQuestionBoundaryRatioForPage(pageNo, q, startRatio)` 공통 함수 추가
  - 경계 후보를 `pageMap/ratioMap`, `질문 시작 앵커`, `locateQuestionInPdf(q+1)`(신뢰 소스) 우선 수집하고,
    실패 시 `questionRatios/row/direct/reliable` 폴백으로 첫 다음 문항 경계를 반환
  - `renderPdfPage()`에서 기존 분산 후보 계산을 제거하고 공통 함수 결과를 하드 상한으로 사용
  - `getQuestionCropRange()`(모바일 세로 포함)도 동일 공통 함수 기반으로 `nextBoundary`를 계산해 모드/경로별 경계 불일치 축소

### 주요 내용 + 2026-03-04 01:35:55
- 단일문항 컷 규칙 명시 반영:
  - `renderPdfPage()`에서 `④` 선택지 하단(`choice4Bottom`)을 직접 계산
  - `safeEnd`에 대해 `q+1 이전(hardEndCapRatio 이하)` 제약을 절대 유지
  - 같은 제약 안에서 `④` 하단이 보이도록 `safeEnd`를 추가 보정(`choice4Bottom + 여유`)

### 주요 내용 + 2026-03-04 01:45:57
- 한문제 보기 전수 안정화 보강(선지 배치 반영):
  - `renderPdfPage()`에서 다음 문항 경계(`provisionalBoundary`)를 먼저 계산한 뒤 선택지 앵커를 해당 경계 내부로 필터링
  - `①~④`의 y-분포(`ySpread`)로 가로형/세로형 선지 배치를 판별해 하단 여유값(`tailPad`)을 차등 적용
  - 경계 탐지/선지 탐지 순서를 정리해, 다음 문항 선지가 현재 문항 ④로 오인되어 컷이 늘어나는 케이스를 완화

### 주요 내용 + 2026-03-04 09:12:36
- 한문제 보기 하단 절단 보정:
  - `maxChoiceBottom` 기반 최소 하단 보장값(`minEndByChoice`) 여유를 확대해 ④줄 문장 하단 잘림 완화
  - 선지 배치별 하단 여유(`tailPad`)를 상향해 가로형/세로형 모두 줄 전체 노출 안정성 개선
  - `④` 직접 보장치(`c4Need`) 여유를 확대해 동그라미 숫자만 보이고 문장 하단이 잘리는 케이스 완화

### 주요 내용 + 2026-03-04 12:30:30
- q+1 경계 오인식 방지 보강:
  - `getNextQuestionBoundaryRatioForPage()`에 `minAfterRatio` 파라미터를 추가해, 지정 기준(④ 하단) 이전 후보를 경계에서 제외
  - `renderPdfPage()`에서 임시 경계 계산 시 `choice4Bottom`을 전달해 선지 숫자(②/③/④)를 다음 문항 시작으로 오인하는 케이스 완화
- 폰트 확대/축소 버튼 라벨 단순화:
  - `글자크게 ⊕` → `글자 ⊕`
  - `글자작게 ⊖` → `글자 ⊖`

### 주요 내용 + 2026-03-01 03:21:57
- PDF 버튼 라벨 단순화:
  - `문제 PDF 새창` → `문제 PDF`
  - `정답 PDF 새창` → `정답 PDF`

### 주요 내용 + 2026-03-01 03:22:51
- 정답키 배지 위치 변경:
  - 패널 내부에서 제거하고 상단 헤더의 테마 변경 버튼 옆으로 이동

### 주요 내용 + 2026-03-01 03:24:24
- 모바일 헤더 액션바 간결화:
  - 다중 줄 그리드에서 단일 행 가로 스크롤 방식으로 변경
  - 버튼/토글 패딩 축소 및 줄바꿈 방지 처리

### 주요 내용 + 2026-03-01 03:25:05
- 모바일 뷰어 네비게이션 버튼 축소:
  - 이전/다음 오버레이 버튼 크기 및 폰트 크기 축소
  - 좌우 오프셋 축소로 화면 점유 면적 완화

### 주요 내용 + 2026-03-01 03:26:16
- 채점 버튼 라벨 단순화:
  - `채점(전체) (G)` → `채점 (G)`

### 주요 내용 + 2026-03-01 03:26:52
- 정답표시 토글 라벨 단순화:
  - `정답표시(강조)` → `정답표시`

### 주요 내용 + 2026-03-01 03:28:23
- 모바일 문항번호 폰트 축소:
  - 모바일 카드의 문항번호(`qnum`) 폰트 크기를 추가 축소해 레이아웃 밀도 개선

### 주요 내용 + 2026-03-01 03:34:33
- 보기모드 상태 뱃지 제거:
  - `전체/한 문제` 상태 배지를 헤더에서 제거
  - 보기모드 토글 버튼(`↕/↔`) 기능은 유지

### 주요 내용 + 2026-03-01 03:35:53
- 모바일 전체보기 문항 위치 보정:
  - 모바일 전체보기에서 한문제 크롭 시작점 보정 로직 제외
  - 모바일은 페이지맵 ratio 우선으로 스크롤 앵커 계산
  - 모바일 전용 ratio 클램프(0.03~0.8) 적용으로 하단/오프셋 오표시 완화

### 주요 내용 + 2026-03-01 03:36:42
- 현재 문항 배지 문구 단순화:
  - `현재 1번` 형식에서 `1번` 형식으로 변경

### 주요 내용 + 2026-03-01 03:38:33
- 모바일 전체보기 문항 이동 안정화(2차):
  - 모바일 전체보기에서 `locateQuestionInPdf` 탐지값 대신 `questionPageMap/questionTopRatioMap` 우선 사용
  - 맵이 없을 때만 기본 페이지 계산으로 fallback
  - 상태 표시에 모바일은 `맵기준`으로 표기

### 주요 내용 + 2026-03-01 03:39:53
- 모바일 전체보기 문항 이동 안정화(3차):
  - 회차 프리셋 앵커(`PRESET_PAGE_ANCHORS`)가 존재하면 모바일 전체보기에서 최우선 적용
  - 프리셋 비율(`ratioMap`)을 함께 적용해 저문항(예: 6번) 위치 오차 완화

### 주요 내용 + 2026-03-01 03:40:34
- 모바일 현재문항 배지 줄바꿈 방지:
  - `currentQInfo`에 최소폭/nowrap/중앙정렬 스타일 추가
  - `1번` 배지가 2줄로 내려가는 현상 완화

### 주요 내용 + 2026-03-01 03:41:21
- 모바일 전체보기 북마크 버튼 위치 조정:
  - 본문 상단 가림을 줄이기 위해 모바일 전체보기에서 하단 우측 고정 배치로 변경
  - 가로 모드에서는 하단 마진을 추가 축소

### 주요 내용 + 2026-03-01 03:42:46
- 모바일 하단 답선택 버튼 선택감 개선:
  - 선택된 번호 버튼(`1~4`)에 active 스타일(배경/테두리/색상) 적용
  - 포커스 변경 및 선택 변경 시 버튼 강조 상태 동기화 로직 추가

### 주요 내용 + 2026-03-01 03:44:17
- 모바일 하단 도크 라벨/뱃지 정리:
  - 하단 문제 번호 배지를 상단 배지(`currentQInfo`)와 동일 스타일로 통일
  - 하단 버튼 문구 `답안` → `답안지`

### 주요 내용 + 2026-03-01 03:48:43
- 모바일 전체보기 하단 컨트롤 정리:
  - 북마커를 하단 도크의 문항 배지 앞에 배치
  - 하단 도크 그리드/폰트/패딩을 조정해 오버플로우 완화
  - 하단 북마커 버튼 활성 상태를 기존 북마크 상태와 동기화

### 주요 내용 + 2026-03-01 03:51:28
- 전체보기 문항 이동 경로 수정:
  - `navigatePdfToQuestion()`에서 전체모드 시 `setPdfFramePage` 우회 경로 제거
  - 전체모드는 `renderAllPdfPages(q)`를 직접 호출해 이전/다음 이동 시 해당 문항 앵커 스크롤 일관성 확보

### 주요 내용 + 2026-03-01 03:55:02
- 모바일 전체보기 하단 이동 보강:
  - `moveFocus()`에서 전체모드 대상 문항 계산 후 `renderAllPdfPages(targetQ)`를 직접 재호출
  - 모바일 답 선택 시에도 문항 위치 동기화를 위해 `navigatePdfToQuestion(q)` 호출 확대

### 주요 내용 + 2026-03-01 03:55:47
- 모바일 세로 모드 네비게이션 버튼 숨김:
  - 세로 모드(`max-width:768` + `portrait`)에서 뷰어 좌우 오버레이 버튼(`‹/›`) 비표시 처리

### 주요 내용 + 2026-03-01 03:56:50
- 모바일 시트 타이틀 문구 단순화:
  - `답안지(모바일)` → `답안지`

### 주요 내용 + 2026-03-01 03:59:09
- 모바일 답안지 상단 2열 정렬 보강:
  - 시트 헤더(`sheetTop`)를 `minmax(0,1fr) auto` 2열로 고정
  - 닫기 버튼 영역(`controls`)에 `justify-self:end`를 추가해 우측 끝 정렬을 강제

### 주요 내용 + 2026-03-01 04:00:57
- 모바일 답안지 문항 선택 이동 UX 개선:
  - 답안지 시트의 문항 카드 클릭 시 해당 문항으로 이동 후 시트를 자동 닫도록 변경
  - 답 선택 버튼(`1~4`) 클릭은 기존처럼 시트를 유지하도록 예외 처리

### 주요 내용 + 2026-03-01 04:02:11
- 모바일 하단 북마커를 뷰어 북마커와 동일화:
  - 하단 북마커 버튼을 뷰어와 동일한 북마크 SVG/스타일(`bookmarkBtn`)로 변경
  - 활성 상태 표시(채움 아이콘)와 토글 동작이 상단 북마커와 동일하게 보이도록 모바일 전용 크기/정렬 스타일 보정

### 주요 내용 + 2026-03-01 04:03:57
- 모바일 답안지 선택 동작 변경:
  - 모바일 답안지 오버레이에서 보기(1~4) 클릭 시 답 선택/저장을 막고 해당 문항으로 즉시 이동 후 시트 닫기
  - 데스크톱/일반 답안지의 기존 답 선택 동작은 유지

### 주요 내용 + 2026-03-01 04:05:14
- 답안지 ESC 닫기 단축키 추가:
  - 답안지 오버레이가 열려 있을 때 `Esc` 키 입력 시 즉시 닫히도록 전역 키다운 처리에 반영

### 주요 내용 + 2026-03-01 04:07:22
- 모바일 전체모드 문항 이동 로직 보강(핵심 수정):
  - 전체보기 문항 이동 스크롤을 `scrollPdfViewportTo()` 공통 함수로 통합
  - `pdfViewer` 내부 스크롤이 불가능한 레이아웃(모바일 전체모드)에서는 `window.scrollTo`로 폴백해 해당 문항 위치로 강제 이동
  - `moveFocus()`의 중복 전체렌더 호출을 제거해 이전/다음 이동 시 레이스/중복 호출 가능성 축소

### 주요 내용 + 2026-03-01 04:11:50
- 모바일 가로(랜드스케이프) 한문제 모드 네비 버튼 축소:
  - 좌우 오버레이 이전/다음 버튼 크기(`32x64`), 폰트(15px), 반경, 좌우 여백을 축소해 화면 점유를 줄임

### 주요 내용 + 2026-03-01 04:14:41
- 배포 환경 모바일 랜드스케이프 답안지 가시성 보강:
  - 랜드스케이프(`max-width:1100`)에서 답안지(`sheet`) 스크롤/패딩을 안정화
  - 답안 카드 그리드를 5열→4열로 조정해 카드 폭 확보
  - 선택지 영역(`choices/choice`)의 표시/폰트/간격을 명시해 숫자(1~4) 비표시 현상 완화

### 주요 내용 + 2026-03-01 04:16:28
- GitHub Pages `Missing PDF` 경로 복구 폴백 추가:
  - PDF URL 해석 시 `decode 1회/2회 + NFC/NFD` 후보를 확장해 경로 정규화 차이를 보정
  - 쿼리 경로가 실패하면 `json/question.json`에서 `questionNo`(또는 회차명/카테고리)로 실제 PDF 경로를 찾아 자동 대체
  - 문제/정답 PDF 모두 동일 복구 로직을 적용

### 주요 내용 + 2026-03-01 04:18:03
- 답안지 상태 문구 UI 제거:
  - `저장 상태`, `모드` 표시 영역을 화면에서 숨김 처리
  - 기존 내부 저장/모드 상태 로직은 유지하고 UI만 비노출

### 주요 내용 + 2026-03-01 04:25:00
- 모바일 가로 전체모드 우측 답안지 공백 이슈 수정:
  - 시트 이동 기준을 `max-width:768`에서 `max-width:768 + portrait`로 변경
  - 모바일 가로(landscape)에서는 시트를 오버레이로 옮기지 않고 우측 패널에 유지하도록 보정
  - 리사이즈 시 오버레이 닫기 조건도 동일 기준으로 통일

### 주요 내용 + 2026-03-01 04:26:25
- 뷰어 문제 이동 버튼 하단 정렬:
  - 좌우 이전/다음 오버레이 버튼의 세로 위치를 중앙 기준에서 하단 기준으로 변경
  - 기본/모바일/모바일 가로(랜드스케이프)에서 하단 여백 값을 각각 보정해 문제 하단에 맞춰 표시

### 주요 내용 + 2026-03-01 04:30:29
- 모바일 랜드스케이프 답안지 헤더/선택지 가독성 개선:
  - 우측 답안지 타이틀과 상단 버튼(정보/맨위로/저장초기화)을 같은 row 한 줄로 고정
  - 우측 패널 폭을 확대(`1.45fr / 1.05fr`)하고 답안 카드 그리드를 `4열 → 3열`로 조정
  - 문항카드/선택지 폰트·패딩·최소높이를 키워 `1~4` 버튼이 너무 좁게 보이던 문제 완화

### 주요 내용 + 2026-03-01 04:34:37
- 모바일 세로 모드 북마크 버튼 위치/크기 조정:
  - 한문제 보기에서 북마크 버튼을 상단 우측에서 하단 우측으로 이동
  - 버튼/아이콘 크기를 축소해 문제 영역 가림을 줄임

### 주요 내용 + 2026-03-01 04:36:47
- PDF 좌우 여백 자동 제거(가독성 개선):
  - 렌더 단계에서 좌우 공백을 감지해 콘텐츠 영역만 확대한 뒤 표시
  - 한문제 모드와 전체보기 모드 모두에 동일하게 적용
  - 과도한 잘림 방지를 위해 최소폭/패딩/무시 임계값을 넣어 안전하게 트리밍

### 주요 내용 + 2026-03-01 04:37:37
- 모바일 세로 북마크 위치 재보정:
  - 화면 최하단이 아닌 뷰어 하단 기준에 맞추기 위해 북마크 `bottom` 값을 상향 조정(`44px`)

### 주요 내용 + 2026-03-01 04:42:56
- 뷰어 하단 우측 `맨위로` 플로팅 뱃지 추가:
  - 표시 조건: `모바일 세로(portrait)` 또는 `비모바일(폭 768px 초과)`에서만 노출
  - `모바일 가로(landscape)`에서는 비노출
  - 뷰어/윈도우 스크롤이 일정 값(90px) 이상일 때만 표시되고 클릭 시 뷰어 최상단으로 이동

### 주요 내용 + 2026-03-01 04:43:41
- 모바일 세로 헤더 고정:
  - `max-width:768` + `portrait` 조건에서 헤더를 `sticky`로 고정해 스크롤 시 상단 유지

### 주요 내용 + 2026-03-01 04:45:59
- 모바일 세로 문제 컨트롤 영역도 고정:
  - `pdfTitleBar`를 헤더 아래 위치에서 `sticky` 고정
  - 기기별 헤더 높이 차이를 반영하기 위해 헤더 실제 높이를 `--mobile-header-h` 변수로 계산해 `top` 오프셋에 적용

### 주요 내용 + 2026-03-01 04:46:54
- 뷰어 `맨위로` 플로팅 버튼 가시성 보정:
  - 조건 모드(모바일 세로/비모바일)에서는 버튼을 항상 표시하도록 변경
  - 상단 위치에서는 `disabled` 스타일로만 약하게 표시하고 클릭 동작은 유지
  - z-index 상향으로 다른 오버레이에 가려지는 경우를 완화

### 주요 내용 + 2026-03-01 04:48:42
- `맨위로` 버튼 위치 방식 수정:
  - 긴 뷰어에서 버튼이 화면 밖으로 내려가던 문제를 해결하기 위해 `absolute` → `fixed` 플로팅으로 변경
  - 모바일 세로에서는 하단 도크와 겹치지 않도록 버튼 하단 오프셋을 상향 조정

### 주요 내용 + 2026-03-01 04:49:43
- `맨위로` 플로팅 강제 적용:
  - 버튼 위치를 `position: fixed !important`로 강제해 레이아웃 영향 제거
  - `z-index`를 상향(60)해 가려짐 방지

### 주요 내용 + 2026-03-01 04:50:49
- 모바일 세로 `글자+/글자-` 컨트롤 바 고정 위치 보정:
  - 상위 패널 `overflow:hidden` 영향으로 sticky가 깨지는 문제를 해결하기 위해 좌측 문제 패널만 `overflow:visible`로 조정
  - `pdfTitleBar`의 sticky 오프셋을 `top:0`으로 단순화하고 동적 헤더 높이 계산 로직 제거

### 주요 내용 + 2026-03-01 04:51:42
- 모바일 세로 `맨위로` 플로팅 버튼 위치 재조정:
  - 하단 컨트롤바와 겹치지 않도록 버튼 하단 오프셋을 추가 상향(`bottom:106px`)

### 주요 내용 + 2026-03-01 04:54:44
- 모바일 세로 컨트롤바 고정 오프셋 재정비:
  - `pdfTitleBar` sticky `top`을 헤더 실측 높이 변수(`--mobile-header-h`)로 재적용
  - `updateStickyOffsets()`를 추가해 초기 로드/리사이즈/회전 시 헤더 높이를 재계산
  - 헤더 높이 변동으로 생기던 컨트롤바 위치 어긋남을 완화

### 주요 내용 + 2026-03-01 04:55:33
- 모바일 세로 `맨위로` 버튼 하단 오프셋 계산식 적용:
  - 버튼 위치를 `하단 컨트롤바 높이 + 15px`(`calc(var(--mobile-dock-h) + 15px)`)로 변경
  - `updateStickyOffsets()`에서 하단 컨트롤바 높이를 실측해 `--mobile-dock-h` 변수로 동기화

### 주요 내용 + 2026-03-01 04:57:26
- 모바일 세로 문제 컨트롤바 고정 방식 전환:
  - `pdfTitleBar`를 `sticky`에서 `fixed`로 변경해 스크롤 시 위치 흔들림 제거
  - 헤더 바로 아래(`top: calc(--mobile-header-h + 2px)`)에 고정
  - 뷰어 본문과 겹치지 않도록 뷰어 상단 패딩(`64px`) 추가

### 주요 내용 + 2026-03-01 04:58:31
- `맨위로` 플로팅 버튼 노출/크기 조정:
  - 버튼 크기를 기본/모바일 세로에서 한 단계 축소
  - 초기에는 숨기고, 스크롤이 일정 값(120px) 이상일 때만 표시되도록 변경

### 주요 내용 + 2026-03-01 05:00:12
- 모바일 세로 상단 액션바 위치 과오프셋 보정:
  - 헤더 실측 높이 기반 오프셋에 상한(clamp)을 적용(`48px~96px`)
  - 비정상적으로 큰 `top` 값으로 컨트롤바가 화면 중간에 뜨던 현상 완화

### 주요 내용 + 2026-03-01 05:01:06
- `맨위로` 버튼 비모바일 상시 노출:
  - 비모바일(폭 768px 초과)에서는 스크롤 여부와 무관하게 플로팅 버튼이 항상 보이도록 분기 조정
  - 모바일 세로의 스크롤 후 노출 동작은 유지

### 주요 내용 + 2026-03-01 05:03:15
- 모바일 세로 상단 컨트롤바-헤더 밀착 정렬:
  - 컨트롤바(`pdfTitleBar`)를 헤더 바로 아래에 `top: var(--mobile-header-h)`로 정렬
  - 좌우 여백/라운드/그림자를 제거하고 풀폭으로 배치해 분리된 카드처럼 보이던 느낌 해소

### 주요 내용 + 2026-03-01 05:04:44
- 모바일 세로 하단 컨트롤바 비표시 보정:
  - `max-width:768` 조건 누락 환경을 보완하기 위해 `orientation: portrait + pointer: coarse`에서 하단 컨트롤바(`mobileDock`)를 강제 표시
  - 동일 조건에서 하단 여백(`padding-bottom:88px`)을 보장해 본문과 겹침 방지

### 주요 내용 + 2026-03-01 05:05:43
- 모바일 세로 헤더-컨트롤바 밀착 보정(2차):
  - 상단 컨트롤바 `top`을 `calc(--mobile-header-h - 8px)`로 조정해 헤더와의 시각적 간격 축소

### 주요 내용 + 2026-03-01 05:09:26
- 모바일 세로 헤더-컨트롤바 밀착 보정(3차):
  - 사용자 요청에 따라 상단 컨트롤바 오프셋을 `-12px`로 추가 조정

### 주요 내용 + 2026-03-01 05:12:39
- PDF 좌우 여백 트리밍 기준 분리:
  - 비모바일에서는 좌우 보존 여백(`2%`)을 추가해 너무 타이트하게 잘리지 않도록 조정
  - 모바일에서는 기존에 가까운 타이트 기준(`0.6%`) 유지
  - 한문제/전체보기 렌더 모두 동일 기준 적용

### 주요 내용 + 2026-03-01 05:14:51
- 비모바일 `맨위로` 버튼 위치 앵커 보정:
  - 비모바일에서 버튼의 화면 기준 우측 고정을 제거하고, `pdfViewer` 우하단 좌표를 실시간 계산해 배치
  - 답안지 쪽으로 붙던 위치 오차를 줄이고 뷰어 영역에 고정되도록 조정

### 주요 내용 + 2026-03-01 05:15:54
- PDF 좌우 여백 보존값 최종 조정:
  - 모바일: 좌우 보존 여백 `0%` (완전 제거)
  - 비모바일: 좌우 보존 여백 `2.5%`
  - 한문제/전체보기 트리밍 로직 모두 동일 적용

### 주요 내용 + 2026-03-01 05:23:46
- 모바일 기본 텍스트 가독성 비율 조정:
  - 모바일 강제 읽기 모드의 기본 PDF 배율 하한을 `1.1` → `1.22`로 상향
  - 초기 진입 시 텍스트가 더 크게 보이도록 개선

### 주요 내용 + 2026-03-01 05:26:06
- 한문제 모드 크롭 안정화 가드 추가:
  - 크롭 시작점이 과도하게 아래로 잡히거나 크롭 높이가 비정상적으로 작으면 범위를 자동 확장해 재보정
  - 초반 문항(1~5번)에서 상단 오검출 시 시작점을 보수적으로 상단으로 되돌리는 추가 가드 적용

### 주요 내용 + 2026-03-01 05:32:43
- 모바일 세로 문항 이동 스크롤 오프셋 보정:
  - 고정 헤더+고정 컨트롤바 적용 후 어긋난 스크롤 기준을 실측 높이 기반으로 재계산
  - `scrollPdfViewportTo/getViewerScrollTop`에서 `getViewerScrollAnchorOffset()`를 사용해 이동 위치 정확도 개선
  - 컨트롤바 높이(`--mobile-toolbar-h`)도 실측 동기화해 뷰어 상단 패딩과 일치시킴

### 주요 내용 + 2026-03-01 05:41:59
- 모바일 가로 모드 액션바 패널 이관 + 답안지 폭 축소:
  - 모바일 가로(`max-width:1100`, `orientation:landscape`, `pointer:coarse`)에서 하단 액션바(`mobileDock`)를 답안지 패널 내부로 이동
  - 패널 내부 모드(`inPanel`) 스타일을 추가해 액션바를 컴팩트 4열로 표시
  - 답안지 패널 폭을 기존 대비 약 50% 수준으로 축소(`1.45fr / 0.52fr`)하여 뷰어 영역 확장
  - 회전/리사이즈 시 `syncLandscapeDock()`로 자동 이동/복귀 처리

### 주요 내용 + 2026-03-01 05:43:02
- 한문제 모드 `맨위로` 플로팅 버튼 비노출:
  - `state.viewMode === "single"`일 때 `viewerTopBadge`를 강제 숨김 처리

### 주요 내용 + 2026-03-01 05:46:26
- 모바일 PDF 좌우 공백 제거 강화:
  - 모바일 트리밍에서 좌우 패딩을 거의 0 수준(`edgePadRatio 0.002`, `edgePadMin 1`)으로 축소
  - 연한 배경 노이즈에 덜 반응하도록 잉크 임계값을 모바일 전용(`232`)으로 강화
  - 한문제/전체보기 공통 트리밍 옵션으로 적용

### 주요 내용 + 2026-03-01 05:47:32
- 모바일 가로 답안지 레이아웃 재조정:
  - 답안지 문항 그리드를 `3열 → 2열`로 변경
  - 가로모드 패널 비율을 `1.55fr / 0.45fr`로 조정해 답안지 폭을 추가 축소하고 뷰어 영역 확대

### 주요 내용 + 2026-03-01 05:48:59
- PDF 공백 제거 로직 정밀 보강:
  - 좌우 트리밍 검출 시 상/하단 노이즈 구간을 제외(`ignoreTop/BottomRatio`)해 페이지 경계선 영향 축소
  - 샘플링 간격(`colStep/rowStep`)과 최소 히트 기준을 강화해 실제 텍스트 영역 중심으로 경계 검출
  - 모바일/비모바일 각각 다른 검출 파라미터를 적용해 모바일 잔여 여백 문제 완화

### 주요 내용 + 2026-03-01 05:49:49
- 답안지 `맨위로` 기능 제거:
  - 답안지 헤더의 `맨위로` 버튼 DOM 삭제
  - 관련 클릭 이벤트 핸들러 제거

### 주요 내용 + 2026-03-01 05:53:30
- 뷰어 좌우 여백 0 기준(전체 조건) 재적용:
  - 뷰어 좌우 패딩을 기본/모바일/가로모드에서 모두 제거
  - 한문제/전체보기 트리밍 옵션을 무여백(`preserveSideRatio=0`, `edgePad=0`)으로 통일
  - 모바일/비모바일 분기 없이 동일한 타이트 트리밍 기준 적용

### 주요 내용 + 2026-03-01 12:24:35
- 모바일 세로 기본 텍스트 크기(배율) 상향:
  - 모바일 읽기 모드 기본 배율 하한을 `1.22` → `1.28`로 추가 상향
  - 첨부 화면과 유사한 가독성 비율에 맞추도록 조정

### 주요 내용 + 2026-03-01 12:36:41
- 모바일 세로 글자 버튼 라벨/폰트 조정:
  - 모바일 세로에서 `글자 ⊕/글자 ⊖`를 `+/-` 기호로 표시
  - `+/-` 버튼에 전용 클래스(`fontSymbolOnly`)를 적용해 기호 폰트 크기 확대
  - 초기 로드 및 리사이즈/회전 시 자동 전환 로직(`updateFontButtonLabels`) 연결

### 주요 내용 + 2026-03-01 12:39:24
- 모바일 가로 글자 버튼도 동일 적용:
  - 모바일 가로(`max-width:1100`, `orientation:landscape`, `pointer:coarse`)에서도 `+/-` 라벨 사용
  - 가로모드 전용 `fontSymbolOnly` 스타일 추가로 기호 폰트 확대 유지

### 주요 내용 + 2026-03-01 12:40:18
- 정답키 뱃지 노출 조건 축소:
  - 정답키 뱃지(`answerKeyInfo`)를 `localhost`에서만 표시하도록 제한
  - `127.0.0.1`, `file:`, `::1` 런타임 분기 제거

### 주요 내용 + 2026-03-01 12:42:26
- 모바일 세로 기본 텍스트 크기 추가 상향:
  - 모바일 읽기 모드 기본 배율 하한을 `1.28` → `1.36`으로 상향

### 주요 내용 + 2026-03-01 12:45:52
- 모바일 가로 모드 자동 풀스크린(베스트에포트) 추가:
  - 대상: `max-width:1100`, `orientation:landscape`, `pointer:coarse`
  - 사용자 첫 제스처 이후 가로 모드 진입 시 풀스크린 요청
  - 가로 모드 이탈 시 풀스크린 자동 해제
  - 사용자가 가로 모드에서 수동 해제한 경우 재강제 진입 방지

### 주요 내용 + 2026-03-01 12:47:27
- 상단 액션 버튼 라벨 단순화:
  - `다시풀기 (R)` → `다시풀기`
  - `채점 (G)` → `채점`

### 주요 내용 + 2026-03-01 12:50:13
- 보기 모드 전환 시 스크롤 탑 적용:
  - 보기 모드(`↔/↕`)를 전환하면 뷰어 스크롤을 항상 상단으로 이동
  - single/all 렌더 완료 시점에서 공통 처리해 기존 자동 위치 이동보다 우선 적용

### 주요 내용 + 2026-03-01 15:38:49
- 모바일 세로 가독성 추가 상향:
  - 모바일 읽기 배율 하한을 세로/가로 분리
  - 세로 모드 하한 `1.48`, 가로 모드 하한 `1.36`으로 적용

### 주요 내용 + 2026-03-01 15:43:02
- 모바일 전체보기 폰트 찌그러짐 비율 보정:
  - 전체보기 좌우 트리밍 캔버스 렌더 시 가로 확장에 맞춰 높이도 비례 재계산하도록 수정
  - single 모드와 동일한 종횡비 보존 방식으로 렌더해 문자 눌림/찌그러짐 완화

### 주요 내용 + 2026-03-01 15:49:44
- 모바일 세로 한문제 모드 문항 표시 안정화:
  - 한문제+세로모드에서 문항 크롭 범위를 단순 앵커 기반(현재/다음 문항 시작점)으로 우선 계산
  - 복잡한 저품질 보정 경로를 우회해 문항 선택 시 해당 문제가 상단부터 안정적으로 보이도록 보정

### 주요 내용 + 2026-03-01 15:54:14
- 모바일/비모바일 줌 저장 분리로 비모바일 폰트 복구:
  - UI 저장값에 `pdfZoomMobile`, `pdfZoomDesktop` 분리 저장
  - 비모바일에서는 데스크톱 전용 줌만 사용(기본값 1), 모바일 강제 가독성 줌이 전파되지 않도록 수정
  - 구버전 공용 `pdfZoom` 값은 모바일에서만 1회 이관

### 주요 내용 + 2026-03-01 16:13:21
- 글자 `원래대로` 기능 추가:
  - 상단 PDF 컨트롤에 `원래대로` 버튼 추가
  - 모바일에서는 `↺` 아이콘 라벨(툴팁: `글자 원래대로`), 비모바일에서는 `원래대로` 텍스트 라벨 적용
  - 리셋 기준: 데스크톱 `1.0`, 모바일 세로 `1.48`, 모바일 가로 `1.36`
  - 현재 배율이 기본값과 같으면 버튼 비활성화

### 주요 내용 + 2026-03-01 16:15:18
- 모바일 토글 아이콘 클릭 시 가로모드 풀스크린 즉시 적용:
  - 보기모드 토글 버튼 클릭 시, 모바일 가로 대상 조건이면 풀스크린 진입을 즉시 시도
  - 기존 자동 풀스크린(회전/리사이즈 기반) 로직과 함께 동작하도록 연계

### 주요 내용 + 2026-03-01 16:16:42
- 모바일 세로 폰트(기본 배율) 추가 상향:
  - 모바일 세로 기본/원래대로 배율을 `1.48` → `1.6`으로 상향
  - 강제 읽기 모드와 `원래대로` 기준이 동일 배율을 사용하도록 일관화

### 주요 내용 + 2026-03-01 16:18:22
- 다크 모드 전환 버튼 위치 조정:
  - 헤더 액션 영역과 분리해 타이틀 우측 끝(헤더 우상단) 고정 배치
  - 모바일/가로모드 브레이크포인트에서도 우상단 정렬 유지

### 주요 내용 + 2026-03-01 16:19:33
- 뷰어 텍스트 선명도 + 모바일 세로 기본 글자 크기 상향:
  - PDF 캔버스 렌더 스케일을 공통 함수로 분리하고 모바일 세로에서 고해상도 렌더(`dpr * 1.45`, 최대 `4`) 적용
  - 기타 모드는 기존 수준 렌더(`dpr * 1.25`, 최대 `2.6`) 유지
  - 모바일 세로 기본/원래대로 배율을 `1.6` → `1.72`로 상향

### 주요 내용 + 2026-03-01 16:21:22
- 테마 버튼 크기 축소:
  - `#btnTheme` 전용 크기를 기본 `30x30`, 모바일 `28x28`로 축소
  - 아이콘(`themeIcon`) 폰트도 함께 축소해 버튼 비율 정렬

### 주요 내용 + 2026-03-01 16:30:27
- 모바일 헤더/컨트롤바 겹침 + 테마 아이콘 여백 정리:
  - `#btnTheme`에 `padding:0`, 중앙 정렬(`inline-flex`) 및 아이콘(`themeIcon`) line-height/margin 정규화 적용
  - 모바일 세로 `.pdfTitleBar` 고정 top 값을 `calc(... - 12px)`에서 `var(--mobile-header-h)`로 변경해 헤더와 겹침 제거
  - 초기 진입 시 `updateStickyOffsets()`를 즉시 + `requestAnimationFrame` 1회 추가 호출해 스티키 오프셋 안정화

### 주요 내용 + 2026-03-01 16:33:56
- 모바일 전용 화면 방향 토글 아이콘 추가:
  - 보기 모드 토글과 분리된 `가로/세로 전환` 버튼(`btnOrientation`)을 헤더 우상단(테마 버튼 왼쪽)에 추가
  - 모바일(`max-width:1100`, `pointer:coarse`)에서만 표시, 상태에 따라 아이콘 `↔/↕` 및 툴팁 자동 변경
  - 클릭 시 세로→가로 전환은 풀스크린 진입 + `screen.orientation.lock('landscape-primary')`를 best-effort로 시도
  - 가로→세로 전환은 `screen.orientation.lock('portrait-primary')` 시도 후 풀스크린 해제

### 주요 내용 + 2026-03-01 16:35:47
- 헤더 타이틀 1줄 고정:
  - `header h1`에 `white-space: nowrap`, `overflow: hidden`, `text-overflow: ellipsis` 적용
  - `header .left`에 `min-width: 0` 추가로 모바일에서 타이틀 줄바꿈 없이 말줄임 처리

### 주요 내용 + 2026-03-01 16:37:08
- 토글 아이콘 구분성 개선:
  - 보기 모드 토글(`↔/↕`)은 유지
  - 가로/세로 방향 토글(`btnOrientation`)은 `↔/↕` 대신 `가/세` 상태형 표기로 변경해 역할 혼동 제거

### 주요 내용 + 2026-03-01 16:40:34
- 모바일 가로 모드 아이콘 위치 충돌 수정:
  - 가로 모드(`max-width:1100`, `orientation:landscape`)에서 `btnOrientation`, `btnTheme`를 `absolute`에서 `static` 배치로 전환
  - 헤더 좌측 타이틀 영역의 우측 여백(`padding-right`)을 0으로 조정해 아이콘/액션 버튼 겹침 해소
  - 두 아이콘을 컨트롤 영역 우측 순서(`order`)로 정렬

### 주요 내용 + 2026-03-01 16:53:23
- 가로 전환 후 세로 복귀되는 문제 보강:
  - 풀스크린 자동 제어의 가로 대상 조건을 `max-width` 제한 없이 `(orientation: landscape) and (pointer: coarse)`로 완화
  - 화면 방향 토글 버튼 노출 조건도 `pointer: coarse` 기준으로 확대
  - 방향 토글로 가로 전환한 상태(`orientationToggleLandscapeActive`)에서는 리사이즈 시 자동 풀스크린 해제를 막아 즉시 세로 복귀 현상 완화

### 주요 내용 + 2026-03-01 17:07:54
- 가로 재진입 불가 상태 안정화:
  - 방향 토글 처리에 `orientationToggleInFlight` 잠금 및 `finally` 해제를 추가해 중복 클릭/실패 후 교착 방지
  - 가로 전환 성공 확인 후(`orientation` 재검사) `orientationToggleLandscapeActive`를 반영하도록 변경
  - `landscape-primary` 잠금 실패 시 `landscape`로 폴백 시도
  - 회전 실패 시 풀스크린을 정리해 다음 가로 전환 시도가 막히지 않도록 보정

### 주요 내용 + 2026-03-03 20:49:52
- 1번 선택 시 문항번호 오인 하이라이트 보정:
  - 선택지 앵커 추출에서 문항 시작 직후 좌측 숫자 토큰(`1.` 등)을 선택지로 오인식하지 않도록 필터 추가
  - 한문제 모드에서 앵커 품질이 낮을 때(검출 수 부족) 파란 선택 하이라이트를 생략해 오표시 방지

### 주요 내용 + 2026-03-03 20:54:28
- 모바일 세로 한문제 컷 범위 재조정(1번 포함):
  - 한문제 모드에서 `nextStart` 미검출 시 과대 컷(`+0.52`)을 제거
  - 선택지 앵커 하단(④ 포함) 기반으로 컷 끝을 우선 결정하고, 다음 문항 시작점이 있으면 그 직전으로 강제 제한
  - 근거 부족 시 보수적 컷(`초반 문항 0.30`, 그 외 0.34) 적용으로 다음 문항 혼입 최소화

### 주요 내용 + 2026-03-03 21:00:55
- 한문제 모드 1번에서 2번이 함께 보이는 회귀 보정:
  - `renderPdfPage`의 컷 범위 강제 확장(초반 문항/짧은 범위 보정)을 저신뢰(`lowConfidence`) 케이스에만 적용
  - 정상 신뢰 문항에서는 과확장 가드를 건너뛰어 다음 문항 혼입을 줄임

### 주요 내용 + 2026-03-03 21:03:07
- 선지 배치 특이 문항(예: 7번) 선택 표시 누락 보정:
  - 선택지 앵커 추출을 2단계로 변경:
    - 1차: 좌측 그룹 우선 추출(오인식 완화)
    - 2차: 선지 검출 수가 부족하면 전체 앵커로 재탐색(세로/다단 배치 대응)
  - 한문제 모드에서 선택 표시 렌더를 과도하게 스킵하던 조기 반환 제거

### 주요 내용 + 2026-03-03 21:08:48
- 뷰어 채점 오버레이(문항번호 동그라미 + 세로선) 추가:
  - 한문제 모드에서 채점 상태(정답/오답)가 확정된 문항에 대해 문항번호 위치 근처에 채점 오버레이를 렌더
  - 정답: 녹색, 오답: 빨간색 계열로 표시
  - 수동 `채점`/자동채점 해제 시에도 뷰어 오버레이가 즉시 반영되도록 `gradeAll`, `clearMarksOnly` 후 현재 문항 재렌더 연계

### 주요 내용 + 2026-03-03 21:13:25
- 전체보기 모드 선택 답안 마킹 동작 추가:
  - 전체보기 렌더에서 페이지별 원본 캔버스(`allPageBaseCanvases`)와 좌표 메타(`allPageCanvasMeta`)를 보관
  - 포커스 문항 이동/답 변경 시 이전 마킹 페이지를 원복한 뒤, 현재 문항 선택 답안에 파란 동그라미 마킹 렌더
  - 키보드 답 선택(`1~4`)도 전체보기에서 즉시 뷰어 재렌더되도록 연계

### 주요 내용 + 2026-03-03 21:15:31
- 뷰어 채점 마크 스타일 변경(요청 이미지 반영):
  - 정답(`ok`): 빨간 원형(기울어진 타원) + 꼬리 스트로크
  - 오답(`bad`): 빨간 사선 스트로크
  - 기존 초록/빨강 동그라미+세로선 스타일을 제거하고 펜 마킹 느낌으로 일원화

### 주요 내용 + 2026-03-03 21:19:31
- 뷰어 클릭 답선택 가로 배열 보정(3/4 선택 누락 대응):
  - `onPdfCanvasClick`에서 선택지 앵커 분포를 분석해 가로 배열 여부를 판별
  - 가로 배열 문항은 x축 근접도 우선 스코어링으로 선택 판정
  - 가로 배열 전용 허용 범위(`xLimit/yLimit`)를 확장해 3·4번 선택 누락 완화

### 주요 내용 + 2026-03-03 21:23:05
- 3번 문항 선택 앵커 오염 보정:
  - 선택지 앵커 재탐색 조건을 `3개 미만`에서 `4개 미만`으로 강화
  - 선택지 후보 y축 분포가 과도하게 벌어지면(문항번호 토큰 혼입 의심), 가장 밀집된 y대역 중심으로 선택지를 재선정
  - 결과적으로 3번 문항의 3/4 클릭 선택 실패 케이스 완화

### 주요 내용 + 2026-03-03 21:25:13
- 답안지 선택 라운드 시각 강조 강화:
  - `.choice.selected`의 테두리/배경/내부 그림자/폰트 두께를 상향해 선택 피드백을 더 또렷하게 조정
  - 다크 테마 전용 `.choice.selected` 색/그림자도 함께 강화

### 주요 내용 + 2026-03-03 21:28:57
- 3번 문항 클릭 선택(3/4) 추가 보정:
  - `onPdfCanvasClick`에서 가로 배열 문항일 때 `클릭 y대역` 우선 필터 후 `x축 최근접`으로 선택하는 직선형 판정 추가
  - 기존 점수 기반 판정은 보조로 유지해 세로/혼합 배열 호환성 유지

### 주요 내용 + 2026-03-03 21:32:15
- 모바일 세로 한문제 보기 우측 잘림 보정:
  - 모바일 세로 한문제 모드의 좌우 트리밍 강도를 완화(`preserveSideRatio/edgePad` 상향)
  - 과크롭 감지 시(`trim.width < 원본의 88%`) 트리밍을 취소하고 원본 폭을 사용해 우측 선지 잘림 방지

### 주요 내용 + 2026-03-03 21:39:07
- 전체보기 모드 전 문항 마킹/채점 오버레이 반영:
  - 기존의 "현재 문항 1개만 원복+마킹" 흐름을 제거하고, `redrawAllModeMarkers()`를 실제 렌더 경로에 연결해 전체 페이지를 기준으로 선택 답안 마킹을 일괄 렌더
  - 전체보기에서 각 문항의 채점 상태(`정답/오답`)가 있을 경우, 뷰어에도 채점 이미지 오버레이를 문항번호 위치에 함께 렌더
  - `gradeAll`, `clearMarksOnly` 이후에도 전체보기일 때 뷰어 재렌더가 즉시 일어나도록 조건을 `single` 전용에서 `single/all`로 확장

### 주요 내용 + 2026-03-03 21:42:22
- 모바일 세로 뷰어 여백/가로 스크롤 동시 보정:
  - `.viewer.single`의 `overflow:hidden`을 제거하고 `overflow:auto` + `-webkit-overflow-scrolling:touch` 적용으로 확대 상태에서 좌우 드래그 스크롤 가능하도록 수정
  - 한문제 모드 모바일 세로의 좌우 트리밍 파라미터를 강화(`preserveSideRatio/edgePad` 0)하여 공백 여백 제거를 우선
  - 과크롭 안전가드 임계값을 `88%` → `84%`로 조정해 불필요한 원본폭 복귀를 줄이고 여백 재발을 완화

### 주요 내용 + 2026-03-03 22:05:53
- 채점 이미지 스타일/좌표 정밀화:
  - 정답 마커를 요청한 첫 번째 샘플 느낌(빨간 손그림 원형 + 꼬리)으로 렌더 스타일 변경
  - 전체모드 채점 위치 오차 개선을 위해 문항 시작 텍스트의 x/y 앵커를 페이지별로 추출하는 `getQuestionStartAnchorsForPage` 추가
  - 기존 고정 좌측 오프셋 기반 위치 계산을 제거하고, 문항 앵커 기반 좌표(`xRatio/yRatio`)로 단일모드/전체모드 채점 마커 모두 정렬
  - 앵커 미검출 시에만 기존 비율맵 기반으로 폴백하도록 보정

### 주요 내용 + 2026-03-03 22:17:56
- 채점 동그라미 모양/위치 추가 보정(1,2,5번 오차 대응):
  - 정답 마커를 단일 손그림 원형+꼬리 형태로 단순화해 샘플 이미지와 유사하게 조정
  - 문항 시작점 앵커 추출을 행 기반뿐 아니라 텍스트 아이템 토큰(`1.`, `2.` 등) 기반으로 확장해 초반 문항 좌표 정확도 강화
  - 채점 마커 X좌표 보정을 우측으로 이동(`+20`)해 좌측 바깥/과좌측 배치 현상 완화
  - 앵커 미검출 시 `questionTopRatioMap` 외에 `getQuestionCropRange(...).focusStart` 폴백을 추가해 전체모드 위치 안정성 보강

### 주요 내용 + 2026-03-03 22:20:06
- 한문제 보기 저품질(근접모드) 회귀 보정:
  - `renderPdfPage`에서 `lowConfidence`일 때 페이지맵(`questionTopRatioMap`) 기반의 보수 컷 규칙을 우선 적용해 문항 누락/과확대 발생을 완화
  - 저품질 구간의 좌우 트리밍 강도를 낮추고(`preserveSide/edgePad` 완화), 과크롭 원복 임계값을 강화(`90%`)해 이전 안정 동작에 가깝게 복원
  - 일반 품질 문항에는 기존 동작을 유지하고, 저품질 분기에만 국소 적용

### 주요 내용 + 2026-03-03 22:26:03
- 정답 채점 마커 모양 재정렬(샘플 기준):
  - 정답 마커를 `열린 손그림 원 + 꼬리` 전용 함수(`drawOkHandMarker`)로 통일해 단일/전체 모드 렌더 일관성 확보
  - 기존 폐곡선 타원 중심 표현을 제거하고, 하단 우측이 살짝 열린 형태와 질감 보강 스트로크를 적용해 요청 이미지 스타일에 가깝게 조정

### 주요 내용 + 2026-03-03 22:33:16
- 정답 마커를 샘플 이미지 형태로 재조정:
  - `drawOkHandMarker`를 보조 질감선 없이 `단일 타원 경로 + 꼬리`로 단순화
  - 타원 회전/반경/시작·종료 각도 및 꼬리 시작점/길이를 조정해 첨부 샘플(손그림 원+꼬리)과 유사한 실루엣으로 보정

### 주요 내용 + 2026-03-03 22:38:30
- 정답 마커 위치 재보정(숫자 겹침 제거):
  - 마커 중심을 문항번호 토큰의 왼쪽 바깥으로 이동하도록 X좌표 계산식 변경(`xAnchor - offset`)
  - Y 오프셋을 소폭 상향 조정해 숫자 중심 겹침을 줄임
  - 단일/전체 모드 동일 좌표식으로 통일 적용
  - 좌표식 보정 과정에서 반경(`radius`) 참조 순서를 정리해 안정성 보강

### 주요 내용 + 2026-03-03 22:50:24
- 채점 마커 렌더 방식을 이미지 오버레이 방식으로 전환:
  - 기존 벡터 경로 직접 드로잉 대신 SVG 데이터 URL 기반 마커 이미지를 생성해 캔버스 `drawImage`로 표시
  - 정답(`ok`)은 샘플 형태의 동그라미+꼬리 이미지, 오답(`bad`)은 사선 이미지로 분리
  - 단일/전체 모드 공통 `drawGradeMarkerImage` 경로로 통일
  - 초기 렌더 누락 방지를 위해 마커 이미지 프리로드(`preloadGradeMarkerImages`) 추가

### 주요 내용 + 2026-03-03 22:54:30
- 정답 마커 꼬리 제거:
  - 정답 SVG 마커에서 오른쪽 아래 대각선 꼬리(path)를 삭제하고 원형 마커만 표시되도록 변경
  - 단일/전체 모드 모두 동일하게 반영

### 주요 내용 + 2026-03-03 23:28:09
- 채점 마커 스타일/모바일 크기 개선:
  - 정답/오답 SVG를 단일선에서 이중 스트로크(외곽 연한선 + 내부 진한선)로 변경해 더 자연스럽고 또렷한 스탬프 느낌으로 보정
  - `drawGradeMarkerImage` 크기 계산에 캔버스 CSS 스케일 보정과 모바일 최소 크기(`ok: 44x34px`, `bad: 24x44px`)를 추가해 모바일에서 너무 작게 보이던 문제 개선
  - 모바일 뷰포트에서는 추가 배율(`mobileBoost`)을 적용해 표시 가독성 강화

### 주요 내용 + 2026-03-03 23:36:04
- 한문제 모드 상하 여백 소폭 추가:
  - `renderPdfPage`의 한문제 컷 범위 계산 직후 상/하 패딩을 추가해 문항 상단/하단이 너무 타이트하게 잘려 보이는 현상 완화
  - 모바일(`max-width:768`)은 `1.2%`, 비모바일은 `0.8%` 패딩 비율 적용(최소 4px)

### 주요 내용 + 2026-03-03 23:42:03
- 한문제 모드 하단 여백 경계 보정:
  - 상하 여백 패딩 추가로 다음 문항이 일부 보이던 회귀(예: 19번에서 20번 일부 노출) 수정
  - 하단 패딩 적용 시, 같은 페이지의 다음 문항 시작점(`questionTopRatioMap[q+1]`) 직전까지만 확장되도록 상한 적용
  - 문항 간 최소 여백(`nextGapPx`)을 유지해 시각적 간격은 확보하면서 다음 문항 혼입 방지

### 주요 내용 + 2026-03-03 23:46:06
- 한문제 모드 문항 경계 하드캡 추가:
  - `safeEnd` 계산 단계에서 같은 페이지의 다음 문항 시작 비율을 절대 상한으로 적용해 다음 문항(예: 20번) 일부 노출을 원천 차단
  - 저품질/일반 품질에 따라 경계 간격(`endGapRatio`)을 차등 적용(저품질 0.010, 일반 0.007)
  - 하단 패딩 상한 보정과 함께 2중 안전장치로 동작

### 주요 내용 + 2026-03-03 23:48:11
- 한문제 모드 상하 여백 추가 확대:
  - 상하 패딩 비율을 모바일 `1.6%`, 비모바일 `1.1%`로 상향
  - 최소 패딩도 `6px`로 상향해 체감 여백 확보
  - 다음 문항 혼입 방지용 경계 하드캡은 그대로 유지

### 주요 내용 + 2026-03-03 23:51:10
- 한문제 모드 다음 문항 혼입 추가 차단:
  - 다음 문항 경계 계산을 페이지맵(`questionTopRatioMap`) 단독 의존에서, 실제 텍스트 시작 앵커(`getQuestionStartAnchorsForPage`) 병행 방식으로 확장
  - 사용 가능한 후보 중 더 이른 시작점(min)을 경계로 사용해 19번에서 20번 일부가 보이던 케이스를 우선 차단
  - 본문 컷(`safeEnd`)과 하단 패딩(`paddedBottom`) 모두 동일 경계값을 사용하도록 정렬

### 주요 내용 + 2026-03-03 23:54:42
- 한문제 모드 선택지 누락 보정:
  - 다음 문항 경계 하드캡 적용 시, 현재 문항 선택지 하단 앵커(`max(y+h)`)를 기준으로 최소 표시 범위를 먼저 확보하도록 수정
  - 경계 캡이 선택지 하단보다 이른 경우(오검출 의심)에는 해당 캡을 무시하도록 예외 처리 추가
  - 최종 `safeEnd`에 선택지 최소 하단 보장을 적용해 선택지 미노출 회귀 방지

### 주요 내용 + 2026-03-03 23:57:17
- 한문제 모드 8번 선택지 누락 케이스 대응:
  - 다음 문항 경계 계산 시 `맵 비율`과 `텍스트 앵커`를 분리 평가하고, 편차가 큰 경우(>0.08) 앵커 오검출로 판단해 맵 우선 적용
  - 경계 출처를 추적(`mapped/both/anchor`)해 앵커 단독 경계일 때는 최소 표시 높이 보장(모바일 0.24, 비모바일 0.20) 우선
  - 결과적으로 선택지 하단이 잘리는 회귀를 줄이면서 다음 문항 혼입 방지 로직은 유지

### 주요 내용 + 2026-03-04 00:00:45
- 한문제 모드 q→q+1 강제 경계 모드 적용:
  - 단일모드 컷 기준을 `현재 문항 시작(q)`부터 `다음 문항 시작(q+1) 직전`으로 우선 고정하는 `sequentialLocked` 경로 추가
  - 시작/다음 경계 후보를 텍스트 앵커, 페이지맵, `locateQuestionInPdf`에서 수집해 가장 이른 유효값으로 결정
  - 강제 경계가 활성화되면 저품질 보정/과확장 보정이 컷을 다시 왜곡하지 않도록 후속 휴리스틱 일부를 비활성화

### 주요 내용 + 2026-03-04 00:04:28
- 한문제 모드 1번 선택지(③/④) 잘림 보정:
  - q→q+1 강제 경계 모드에서 `다음 문항 시작 유효 간격`을 상향
  - 초반 문항(q<=10)은 `start + 0.18`, 그 외 문항은 `start + 0.14` 이상일 때만 다음 경계로 인정
  - 너무 이르게 잡힌 q+1 경계(오검출)로 인해 선택지 하단이 잘리던 문제 완화

### 주요 내용 + 2026-03-04 00:08:05
- 한문제 모드 1번 선택지 잘림 추가 보정:
  - `q→q+1` 경계 계산에서 `ratioMap`을 강한 후보로 쓰지 않고, 텍스트 앵커/`locateQuestionInPdf` 실측 후보를 우선 사용하도록 변경
  - 실측 후보가 없을 때만 `ratioMap` 폴백 사용
  - 초반 문항 최소 다음 경계 간격을 `0.22`(기존 0.18)로 상향해 조기 컷으로 인한 ③/④ 누락 완화

### 주요 내용 + 2026-03-04 00:12:29
- 단일모드 컷 로직 정리(회귀 복구):
  - `renderPdfPage`에 누적된 다중 휴리스틱(`sequentialLocked` 등)을 제거하고, `getQuestionCropRange` 결과를 우선 신뢰하는 단순 경로로 복구
  - q+1 경계는 실측(텍스트 앵커/locate) 우선, 필요 시에만 `ratioMap` 폴백 사용
  - 선택지 하단 최소 보장을 유지하면서 경계 캡은 보수적으로 적용해 `선택지 잘림`과 `다음 문항 혼입`을 동시에 완화

### 주요 내용 + 2026-03-04 00:19:43
- 단일모드 다음 문항 섞임 회귀(3→4, 5→6, 7→8, 15→16) 보정:
  - 실측 q+1 경계 탐지 실패 시에도 `q+1` 페이지맵 비율을 하드 상한으로 적용
  - 최소 표시 높이 보정(minSpan) 적용 시에도 상한을 넘지 않도록 제한
  - 하단 패딩 경계 역시 동일 상한(실측 우선, 실패 시 맵)으로 통일해 컷/패딩 불일치 제거

### 주요 내용 + 2026-03-04 00:24:41
- 단일모드 빈 화면 회귀 방지 안전장치 추가:
  - `safeEnd - safeStart`가 임계치(모바일 0.18 / 비모바일 0.14)보다 작아지면 `focusStart/focusEnd` 범위로 자동 폴백
  - 폴백 후에도 최소 컷 높이를 강제해 캔버스가 상단 얇은 줄만 보이는 blank 상태 방지

### 주요 내용 + 2026-03-04 00:33:17
- 한문제 모드 컷 붕괴/선택지 누락 동시 보정:
  - 하단 패딩 경계 적용 시 `다음 문항 시작값`이 현재 문항보다 위로 잘못 들어오는 경우를 차단
  - 경계 적용 최소 높이(`minVisiblePx`)를 두어 1px 수준으로 잘리는 blank/반쪽 렌더 회귀 방지
  - 선택지 앵커가 일부만 검출된 문항은 최소 표시 높이 바닥값(`choiceFloor`)을 적용해 ③/④ 누락 완화

### 주요 내용 + 2026-03-04 00:37:49
- 한문제 모드 선택지 ③/④ 누락(예: 8번) 추가 보정:
  - 선택지 앵커가 4개 미만일 때 컷 최소 높이(`choiceFloor`)를 상향해 하단 선택지 노출 우선
  - 최소 표시 높이(`minWindow`)도 앵커 부족 케이스에서 더 크게 보장
  - 단, `nextStartRaw` 상한은 유지해 다음 문항 혼입 없이 확장되도록 제한

### 주요 내용 + 2026-03-04 12:46:25
- 한문제 보기 컷 경계 보정: `q+1` 경계 후보 필터를 완화하여 촘촘한 문항(특히 선택지 ④ 하단과 다음 문항 시작 간격이 작은 경우)에서 다음 문항 경계를 놓치지 않도록 수정.
- 경계 판정 보정: `isValidNextStart`의 `maxChoiceBottom` 근접 배제 임계값을 축소해 실제 다음 문항 시작점이 오검출로 버려지지 않게 조정.
- 경계 미검출 fallback 보정: 세로형 선택지 레이아웃에서 하단 여유(`tailPad`)를 더 타이트하게 줄여, 다음 문항 상단 혼입을 방지.

### 주요 내용 + 2026-03-04 12:58:36
- 세로 배열/2열 선택지 컷 오류 보정: `getChoiceAnchorsForQuestion(pageNo, q, options)`로 확장하여 `startRatio/endRatio` 범위 제한을 지원, 다음 문항 선택지가 현재 문항 선택지로 섞이는 문제를 차단.
- 한문제 보기 경계 순서 수정: 경계 힌트(`q+1` 시작) 선탐색 후 선택지 앵커를 추출하도록 변경하여 `15번→16번`, `24번→25번`처럼 다음 문항 혼입되는 케이스를 완화.
- 선택지 배치 판정 보정: y-밀집대역 재선정 트리거를 `spread > 0.08`에서 `> 0.16`으로 상향해, 정상적인 2열/세로 배열을 오검출로 잘못 압축해 ④가 잘리는 현상 방지.
- 모바일 세로 한문제 분기에서도 동일 범위 제한을 적용해 선택지 ④ 누락/다음 문항 혼입 재발을 줄임.

### 주요 내용 + 2026-05-29 12:38:42
- 방통대 문제은행 구축 기반 추가:
  - 기존 문제은행 manifest는 유지하고 `knou_group/category/question/course_map/answer_sources` 별도 manifest 추가
  - 메인 화면에 기존/방통대 과목 통합 검색 추가
  - `category.html`, `quiz.html`에 `dataset=knou` 분기와 방통대 통합 정답표 참조 로딩 추가
  - 다중정답(A~K) 배열 정답 판정 및 35/70/100 등 회차별 문항 수 대응 추가
  - `scripts/import-knou.mjs` 추가: PDF/HWP 정답표 인덱싱, 문제 PDF 파일명 추론/복사, knou manifest 갱신 지원

### 주요 내용 + 2026-05-29 15:01:36
- 로컬 관리자 서버/화면 추가:
  - `scripts/admin-server.mjs`와 `admin.html` 추가로 Node 로컬 서버에서 문제은행 파일 목록, 디렉토리 생성, 파일 업로드, 이름 변경을 지원
  - 직접 업로드는 `pdf/`, `json/` 하위로 제한하고 저장 디렉토리/파일명을 지정할 수 있게 구성
  - 방통대 문제/정답 업로드는 `scripts/import-knou.mjs`를 호출해 기존 규칙대로 파일 복사와 `knou_` manifest 갱신을 수행
  - GitHub 반영 전 로컬 변경사항 확인을 위해 관리자 화면에 `git status --short` 조회 기능 추가

### 주요 내용 + 2026-05-29 22:15:07
- pdf 디렉토리 스캔 기반 manifest 생성 구조 반영:
  - `scripts/gen_category.py`, `scripts/gen_question.py`, `scripts/gen_correct.py`와 공통 유틸 추가
  - `pdf/` 하위 디렉토리를 카테고리/서브카테고리로 읽고, 루트/카테고리별 `question.json`, `correct.json` 생성
  - 동일 디렉토리 정답 파일을 우선 매핑하고, 방통대는 `pdf/방통대/정답/{시험유형}/{년도}`에서 년도/학기 기준으로 정답표 매핑
  - 사이트는 `correct.json`이 연결된 회차만 퍼블리싱된 문제로 표시하고, `quiz.html`은 `correct` 파라미터로 정답 JSON을 직접 로딩
  - 관리자 화면에 pdf 기준 JSON 재생성 버튼 추가

### 주요 내용 + 2026-05-29 23:01:30
- PDF 문제지 실시간 패턴 탐지 공통화:
  - `quiz.html`에서 PDF 텍스트 레이어를 분석해 문항 번호, 선택지, 헤더/푸터, 페이지별 문항 시작점을 실시간으로 산출
  - 분석 결과를 PDF source/version/questionCount 기준으로 저장하고 같은 PDF 재진입 시 캐시/저장 매핑을 재사용
  - 기존 농산물품질관리사 preset은 삭제하지 않고 실시간 분석 실패 시 fallback으로만 사용
  - 한문제 컷 계산에서 실시간 문항 위치맵과 푸터 제외 영역을 사용하고, 저신뢰 문서는 기존 넓은 fallback으로 표시
  - 농산물품질관리사 2025년 1차 1/5/10/50/100번 이동과 방통대 25문항 PDF fallback 표시를 브라우저에서 확인

### 주요 내용 + 2026-05-29 23:05:06
- 정답 JSON 포맷 정리:
  - `answers` 배열 저장 형식을 10개 단위 줄바꿈으로 통일
  - `scripts/pdf_manifest_lib.py`, `scripts/generate-answer-json.mjs`, `scripts/apply-gpt-answers.mjs`, `scripts/import-knou.mjs`의 정답 배열 출력 로직 변경
  - 기존 `correct.json` 및 회차별 `최종정답.json` 파일을 10개 단위 포맷으로 재저장

### 주요 내용 + 2026-05-29 23:14:13
- 방통대 PDF 인쇄 문항번호 오프셋 대응:
  - `quiz.html`에서 화면 내부 문항 번호와 PDF 인쇄 문항 번호를 분리하고 `16~30번`, `36번 시작` 같은 회차를 자동 매핑
  - URL/manifest의 `questionStartNo`, `questionEndNo`를 우선 사용하고, 값이 없어도 PDF 헤더의 `16～30번` 범위와 문항 라벨로 시작 번호 추론
  - 2컬럼 방통대 문제지의 오른쪽 컬럼 문항번호까지 탐지하도록 x범위를 확장하고, 컬럼 전환 시 이전 문항 위치로 시작점이 밀리는 문제 보정
  - `category.html`, `scripts/gen_question.py`, `scripts/import-knou.mjs`에 인쇄 문항 범위 전달/생성 로직 추가
  - 브라우저에서 방통대 2016년 출석수업대체시험 16~30번 회차와 농산물품질관리사 2025년 100번 이동을 확인

### 주요 내용 + 2026-05-29 23:31:04
- 관리자 문제 등록 프로세스 점검 화면 추가:
  - `admin.html`에 `파일 배치 → JSON 생성 → 결과 확인` 흐름을 한눈에 보는 프로세스 패널 추가
  - `scripts/admin-server.mjs`에 `/api/admin/process-report`를 추가해 문제 PDF, 정답 파일, `correct.json`, 게시/미게시 회차를 집계
  - 생성 스크립트(`gen_category.py`, `gen_question.py`, `gen_correct.py`)와 필수 도구(`python3`, `pdftotext`, `hwp5html`) 준비 상태를 관리자에서 확인하도록 구성
  - `프로세스 점검`, `점검 후 JSON 재생성` 버튼으로 새 디렉토리/파일 추가 이후 수행할 작업을 관리자 화면에서 바로 관리

### 주요 내용 + 2026-05-29 23:56:34
- 관리자 화면 테이블/OCR 관리 구조 재구성:
  - `admin.html` 첫 화면을 카테고리 테이블과 문제 회차 테이블 중심으로 재구성하고 검색, 구분, 게시 상태, 카테고리 필터를 추가
  - 각 문제 회차에서 원본 문제 PDF, 정답 PDF, 풀이 화면, 문제/정답 PDF 진단을 바로 실행하도록 구성
  - `scripts/admin-server.mjs`에 `/api/admin/catalog`, `/api/admin/pdf-info`, `/api/admin/ocr` 추가
  - PDF 진단은 `pdfinfo`, `pdftotext`로 페이지 수, 텍스트량, 문항번호 감지 수, OCR 필요 경고, 텍스트 샘플을 표시
  - OCR 적용은 `ocrmypdf` 기반으로 `기존 OCR 재작성`, `전체 강제 OCR`, `텍스트 없는 페이지만 OCR` 방식을 지원하고 `_ocr.pdf` 사본 생성 또는 원본 백업 후 교체를 선택하도록 구성

### 주요 내용 + 2026-05-30 00:08:18
- 관리자 처리 진행/확인 UX 추가:
  - JSON 재생성, 점검 후 JSON 재생성, OCR 적용, 업로드, 이름 변경처럼 파일이나 JSON을 변경하는 작업은 실행 전 확인 다이얼로그를 표시
  - 프로세스 점검, PDF 진단, OCR 적용, JSON 재생성, 업로드, 이름 변경, 디렉토리 생성, Git 상태 확인에 공통 진행 모달과 단계 상태를 표시
  - 이름 변경은 브라우저 기본 prompt 대신 경로 입력 다이얼로그를 사용하도록 변경
  - 브라우저에서 JSON 재생성 확인 다이얼로그와 프로세스 점검 진행 모달 표시/자동 닫힘을 확인

### 주요 내용 + 2026-05-30 00:10:34
- 관리자 정보성 토스트 알림 추가:
  - 완료, 취소, 오류, 확인 필요 메시지를 우측 하단 토스트로 표시하는 공통 UI 추가
  - 진행 모달 완료/실패, 확인 다이얼로그 취소, PDF 선택 누락, 관리자 서버 오류에 토스트 알림 연결
  - 브라우저에서 JSON 재생성 취소 토스트와 프로세스 점검 완료 토스트 표시를 확인

### 주요 내용 + 2026-05-30 00:22:48
- 관리자 문제/정답 PDF 버튼 반응 개선:
  - 문제 회차 행의 `문제`, `정답` 클릭이 무반응처럼 보이지 않도록 `문제 보기`, `정답 보기` 버튼으로 정리
  - 버튼 클릭 시 오른쪽 PDF 패널에 원본 PDF iframe 미리보기, 대상 경로, 안내 문구, 토스트를 표시하도록 변경
  - 원본 PDF를 크게 보는 동작은 별도 `새 탭으로 열기` 버튼으로 분리
  - `문제 진단`, `정답 진단`은 텍스트 레이어/OCR 상태를 검사하는 버튼으로 유지
  - 관리자 HTML 제공 결과와 스크립트 문법에서 PDF 미리보기/토스트 연결을 확인

### 주요 내용 + 2026-05-30 00:31:59
- 관리자 PDF/OCR 진단 기준 보강:
  - `/api/admin/pdf-info`에서 `question.json`, `knou_question.json`의 예상 문항 수를 찾아 PDF 진단에 반영
  - 문항번호 총 감지 수, 고유 문항번호 수, 문항번호 범위, 선택지 기호 감지 수를 추가 산출
  - 예상 문항 수 대비 고유 문항번호 부족, 문항번호 과다 감지, 선택지 기호 부족을 OCR 품질 경고로 표시
  - 2023년 제20회 농산물품질관리사 원본 PDF와 `_ocr.pdf`가 모두 경고 상태로 잡히고, 2022년 정상 PDF는 경고가 없는 것을 API로 확인

### 주요 내용 + 2026-05-30 00:36:21
- 관리자 회차 리스트 OCR 상태 반영:
  - `/api/admin/catalog` 회차 행 상태에 PDF 진단 결과를 연결해 게시 완료 회차라도 OCR 문제가 있으면 `OCR 확인 필요`로 표시
  - 회차 리스트 응답에 `fileIssue`, `ocrIssue`, `needsOcr`, 문항번호/선택지 감지 수, OCR 경고 목록을 포함
  - PDF 진단 결과는 파일 경로/크기/수정시간 기준으로 캐시해 반복 새로고침 시 재분석 부담을 줄임
  - 2023년 제20회 농산물품질관리사 원본과 `_ocr.pdf`가 회차 리스트에서도 `OCR 확인 필요`로 내려오고, 2022년 정상 PDF는 `정상`으로 유지되는 것을 확인

### 주요 내용 + 2026-05-30 00:44:31
- 관리자 회차 선택/진단 레이아웃 재구성:
  - 문제 회차 행을 클릭/포커스하면 선택 상태가 표시되고 오른쪽 `선택 회차 상세 / 진단 결과` 패널이 갱신되도록 변경
  - 문제 회차 목록 컬럼을 `상태 / 회차명 / 문제 진단 / 정답 진단 / 문항 / 작업` 구조로 정리
  - `/api/admin/catalog`에 문제 PDF 진단과 정답 파일 진단 상태를 별도 필드로 포함하고, 정답 PDF는 정답 번호/값 감지 수로 품질을 판단
  - 오른쪽 상세 패널에서 문제 진단 경고와 정답 진단 경고를 한눈에 볼 수 있도록 요약 박스 추가
  - 2023년 제20회 농산물품질관리사 문제는 문제 진단 `OCR 확인 필요`, 정답 진단 `정상`으로 내려오고, 2022년 정상 회차는 둘 다 `정상`인 것을 확인

### 주요 내용 + 2026-05-30 00:47:44
- 관리자 화면 작업대 구조 재배치:
  - `선택 회차 상세 / 진단 결과` 패널을 우측 최상단 고정 영역으로 이동해 회차 목록 선택 후 바로 확인 가능하도록 변경
  - 우측 작업대에 sticky 레이아웃과 제한 높이 스크롤을 적용해 회차 목록을 내려도 선택 회차 상세와 PDF 진단 작업을 유지
  - 운영 점검, JSON 재생성, Git 상태는 `운영 도구` 접힘 영역으로 묶어 기본 회차 관리 흐름에서 분리
  - 좁은 화면에서는 우측 작업대 고정을 해제해 한 열 레이아웃으로 자연스럽게 전환

### 주요 내용 + 2026-05-30 00:48:40
- 관리자 회차 목록 파일명 표시 개선:
  - 문제 회차 목록의 `회차명` 컬럼을 `문제 파일`로 변경하고 실제 문제 PDF 파일명을 대표 텍스트로 표시
  - 전체 PDF 경로는 목록에서 길게 반복하지 않고 폴더 경로만 보조 정보로 표시
  - 선택 회차 상세에는 `문제 파일명`과 기존 `등록명`을 함께 표시해 관리 기준과 내부 이름을 구분
  - 검색 입력 안내 문구도 `카테고리, 과목명, 파일명 검색`으로 변경
  - 좁은 브라우저 폭에서는 선택 회차 상세 작업대가 회차 목록 아래로 밀리지 않도록 목록보다 먼저 배치하고 sticky 상태를 유지

### 주요 내용 + 2026-05-30 00:53:48
- 관리자 OCR 대상 자동 선택 보정:
  - 문제 회차를 선택하면 해당 문제 PDF를 우측 상세 패널의 기본 작업 대상으로 자동 지정
  - PDF 진단, 새 탭 열기, OCR 적용 실행 시 대상 입력이 비어 있으면 선택 회차의 문제 PDF를 자동으로 채움
  - 문제/정답 보기나 문제/정답 진단을 직접 누른 경우에는 선택한 PDF 역할과 회차 소유 정보를 함께 저장
  - OCR 적용 버튼만 눌러도 선택 회차가 있으면 `PDF를 먼저 선택하세요`로 막히지 않도록 개선

### 주요 내용 + 2026-05-30 00:58:18
- 관리자 OCR 명령 옵션 보정:
  - `ocrmypdf --redo-ocr` 모드에서 `--deskew`를 함께 넘기면 실패하는 문제 수정
  - `redo` 모드는 기존 OCR 재작성에 집중하도록 `--deskew`를 제외
  - `force`, `skip` 모드는 기존처럼 `--deskew`를 포함해 스캔 보정 옵션을 유지
  - 관리자 서버 재시작 후 `/api/admin/catalog` 응답을 확인

### 주요 내용 + 2026-05-30 01:04:04
- 관리자 회차 상세/진단 별도 페이지 분리:
  - `admin-detail.html`을 추가해 선택 회차 정보, 문제/정답 진단, PDF 미리보기, OCR 적용을 전담하도록 구성
  - `admin.html` 메인 목록에서는 우측 상세 패널을 제거하고 `상세/진단` 링크로 별도 페이지 이동
  - 메인 관리자 화면은 카테고리/문제 회차 목록과 운영 도구 중심으로 단순화
  - 별도 상세 페이지에서 문제 PDF/정답 PDF 전환, PDF 진단, OCR 적용, OCR 결과 미리보기와 회차 상태 재조회 지원

### 주요 내용 + 2026-05-30 01:09:43
- OCR 정상 여부를 게시 조건에 반영:
  - 관리자 API에서 `published` 판정 시 정답 JSON뿐 아니라 문제 PDF OCR 진단 상태가 `정상`인지 함께 확인
  - `published:false`가 명시된 회차는 `correctJson`이 있어도 게시하지 않도록 `isPublishedQuestion` 기준 수정
  - `index.html`, `category.html`에서도 `ocrPublished:false` 또는 비정상 `ocrStatus` 회차를 퍼블리싱 목록에서 제외
  - `scripts/gen_correct.py`가 정답 JSON 생성 후 문제 PDF OCR 품질을 점검해 비정상 회차는 `published:false`, `ocrStatus: OCR 확인 필요`로 기록
  - 2023년 제20회 농산물품질관리사 회차가 관리자 catalog/process-report에서 미게시 및 `OCR 확인 필요`로 표시되는 것을 확인

### 주요 내용 + 2026-05-30 01:14:34
- 상세 페이지 PDF 진단 결과 표시 개선:
  - `admin-detail.html`의 PDF 진단 결과를 단순 텍스트 로그에서 `판정 / 핵심 지표 / 경고 / 텍스트 샘플` 구조로 변경
  - 진단 결과 상단에 `정상` 또는 `OCR 확인 필요` 판정을 명확히 표시
  - 페이지 수, 텍스트량, 한글 글자 수, 문항번호 감지, 선택지 기호 감지, OCR 사본 여부를 지표 박스로 분리
  - 경고 항목과 텍스트 샘플을 별도 영역으로 보여 진단 근거를 바로 확인 가능하도록 개선

### 주요 내용 + 2026-05-30 01:19:09
- 상세 페이지 OCR 진단 메시지 보강:
  - PDF 진단 결과 카드에 `OCR이 정상입니다.` 또는 `OCR 확인이 필요합니다.` 상황 메시지를 별도 강조 영역으로 표시
  - 같은 판정 메시지를 토스트 알림으로도 표시해 진단 완료 직후 상태를 바로 인지할 수 있도록 개선
  - 기존 상세 설명은 보조 문구로 유지해 판정과 근거를 구분

### 주요 내용 + 2026-05-30 01:39:03
- 상세 페이지 PDF 탭/OCR 진행 상태 개선:
  - `admin-detail.html`의 PDF 진단/OCR 대상 선택을 `문제 PDF`/`정답 PDF` 탭 전환 구조로 변경
  - `scripts/admin-server.mjs`의 OCR 적용을 동기 응답 대기 방식에서 백그라운드 작업 생성/상태 조회/취소 API 방식으로 변경
  - OCR 적용 모달에 진행률, 경과 시간, 처리 페이지 수, 최근 로그, 실패 메시지, 취소 버튼을 표시
  - `ocrmypdf`가 퍼센트를 출력하면 실제 퍼센트를 사용하고, 없으면 페이지 처리 로그와 단계 로그 기반 추정 진행률을 표시
  - 실패/취소 시 진단 결과 영역에 최근 로그와 오류 내용을 유지해 원인을 확인할 수 있도록 보정

### 주요 내용 + 2026-05-30 01:46:37
- OCR 장시간 작업 진행 표시 보강:
  - `ocrmypdf` 실행 시 `PYTHONUNBUFFERED=1`과 verbose 로그를 적용해 중간 로그 수신 가능성을 높임
  - 페이지별 로그가 아직 출력되지 않는 동안에도 경과 시간 기반 추정 진행률을 표시
  - 진행 문구에서 `페이지 0/18` 대신 `페이지 로그 대기/18`로 상태를 구분
  - OCR 작업 목록 조회 API(`/api/admin/ocr/jobs`)를 추가해 서버가 들고 있는 작업 상태를 확인할 수 있게 구성
  - OCR 취소 시 하위 프로세스가 남지 않도록 프로세스 그룹 종료와 강제 종료 fallback을 적용
  - 이전 코드로 실행 중이던 장시간 OCR 프로세스를 종료하고 관리자 서버를 재시작

### 주요 내용 + 2026-05-30 02:06:25
- OCR 취소/서버 연결 실패 대응 보강:
  - `Failed to fetch` 발생 시 관리자 서버 연결 오류로 안내하고 OCR 상태 조회는 즉시 종료하지 않고 재시도하도록 변경
  - 상태 조회가 실패해도 OCR 진행 모달의 취소 버튼을 유지
  - 서버 재시작으로 작업 ID를 잃은 경우에도 현재 PDF 경로 기준으로 실행 중인 `ocrmypdf` 프로세스를 찾아 종료
  - OCR 로그/stdout/stderr의 제어 문자를 정리해 관리자 API 응답 안정성을 높임
  - 작은 정답 PDF로 OCR 완료 테스트를 수행해 `_ocr.pdf` 생성과 텍스트 추출을 확인

### 주요 내용 + 2026-05-30 02:19:56
- OCR 장시간/중복 실행 상태 표시 개선:
  - 같은 PDF에 OCR 작업이 이미 실행 중이면 새 작업을 만들지 않고 기존 작업에 연결
  - 한글 파일명의 NFC/NFD 차이로 같은 PDF가 다른 경로처럼 인식되어 중복 실행되는 문제 보정
  - 페이지 처리가 끝난 뒤 로그 없이 시간이 늘어나는 구간을 `PDF 후처리 중`으로 표시
  - OCR 실행 중에는 `OCR 적용` 버튼을 비활성화하고, 실패 시 다시 활성화되도록 보정

### 주요 내용 + 2026-05-30 02:31:45
- OCR PDF 생성 fallback 적용:
  - `scripts/ocr-fallback.mjs`를 추가해 `pdftoppm -> tesseract PDF -> pdfunite` 순서로 OCR PDF를 생성
  - 관리자 OCR 기본 엔진을 fallback 파이프라인으로 전환해 `ocrmypdf` 후처리 지연으로 `_ocr.pdf`가 생성되지 않는 문제 회피
  - fallback 진행 로그(`FALLBACK page n/total`)를 관리자 진행률에 반영
  - 2023년 제20회 농산물품질관리사 1차 문제지에서 실제 `_ocr.pdf` 파일 생성과 텍스트 추출을 확인

### 주요 내용 + 2026-05-30 03:02:18
- OCR 품질 진단 기준 보강:
  - `_ocr.pdf` 생성 성공과 OCR 텍스트 품질 통과를 분리해 판정
  - `/api/admin/pdf-info`에서 선택지 정상 기호 수, OCR 변형 선택지 토큰 수, 한글 음절 분리 구간 수를 함께 반환
  - 한글이 음절 단위로 과도하게 분리되거나 선택지 기호가 `0)`, `Q@`, `©`, `®` 같은 변형 토큰으로 깨지면 `OCR 확인 필요`로 유지
  - 정상 구조가 충분히 감지되는 PDF는 페이지당 텍스트량이 적어도 무조건 실패 처리하지 않도록 보정
  - 상세 관리자 화면에 `OCR 변형`, `한글 분리` 지표와 품질 실패 메시지를 표시
  - OCR 적용 후 PDF가 생성돼도 진단 품질 기준을 통과하지 못하면 완료가 아닌 `OCR 품질 확인 필요`로 안내
  - 문제 풀이 화면의 실시간 패턴 분석은 깨진 선택지 토큰을 보조 신호로 사용하되, 관리자 품질 판정은 정상 기호와 분리해서 유지

### 주요 내용 + 2026-05-30 07:15:42
- OCR 파생 PDF 관리 기준 보강:
  - `scripts/pdf_manifest_lib.py`에서 `_ocr`, `_ocr_fixed`, `_fixed`, `_original` PDF를 문제/정답 원본 후보에서 제외
  - 관리자 API에서 OCR 결과/백업 PDF를 `OCR 파생 파일`로 판정하고 게시 대상에서 제외
  - `/api/admin/pdf-info`가 파생 PDF 여부와 원본 후보 경로를 함께 반환하도록 구성
  - `/api/admin/ocr/start`는 파생 PDF에 다시 OCR을 적용하지 않고 원본 PDF에서 실행하라는 오류를 반환
  - 상세 관리자 화면에서 파생 PDF 선택 시 원본 후보를 안내하고 `OCR 적용` 버튼을 눌러도 실행하지 않도록 차단

### 주요 내용 + 2026-05-30 07:29:18
- OCR 결과 메시지 분리:
  - `/api/admin/pdf-info`의 OCR 품질 경고와 파생 PDF 관리 안내를 `qualityWarnings`/`managementWarnings`로 분리
  - `needsOcr` 판정은 OCR 품질 경고만 기준으로 계산
  - 상세 관리자 화면에서 `OCR 품질 경고`와 `관리 안내`를 별도 섹션으로 표시
  - 원본 PDF에서 OCR을 실행해 `_ocr.pdf`가 생성된 경우 품질 실패 메시지에 `원본에서 다시 실행` 안내가 섞이지 않도록 보정
  - OCR 진행 문구를 특정 엔진명 대신 일반 `OCR 작업` 기준으로 표시

### 주요 내용 + 2026-05-30 07:38:55
- OCR 파생 회차 원본 이동 개선:
  - 상세 관리자 화면에서 `_ocr`, `_ocr_fixed` 같은 파생 PDF 회차를 열면 원본 후보와 `원본 회차 열기` 링크를 함께 표시
  - 파생 PDF에 `OCR 적용`을 누른 경우에도 같은 원본 이동 안내를 표시
  - 원본 후보 PDF가 catalog에 있으면 해당 `admin-detail.html?dataset=...&questionNo=...` 링크로 바로 이동 가능하도록 구성

### 주요 내용 + 2026-05-30 07:47:31
- OCR 품질 실패 후 대상 유지 보정:
  - 원본 PDF에서 OCR을 실행한 뒤 결과 PDF가 품질 기준을 통과하지 못하면 `_ocr.pdf`를 적용 대상처럼 선택하지 않도록 변경
  - 품질 실패 시 `OCR 결과 파일 생성됨(적용 보류)`와 `원본 PDF 유지`를 표시하고 현재 작업 대상은 원본 PDF로 유지
  - OCR 진행 모달에서 페이지 처리가 끝난 뒤에는 취소 버튼을 숨겨 완료/실패 상태와 버튼 의미가 섞이지 않도록 보정

### 주요 내용 + 2026-05-30 07:59:12
- 정상 OCR PDF 생성 파이프라인 개선:
  - `scripts/build-layered-ocr-pdf.py`를 추가해 300dpi 렌더 이미지 위에 정리된 OCR 텍스트 레이어를 직접 얹는 방식으로 변경
  - `scripts/ocr-fallback.mjs`가 `tesseract pdf` 대신 페이지별 `txt`를 생성하고, 정리된 텍스트 레이어 PDF를 생성하도록 수정
  - OCR 텍스트 후처리에서 한글 음절 단위 공백을 줄이고 선택지 OCR 변형 토큰을 `①②③④`로 보정
  - 관리자 OCR 기본 적용 방식을 `_ocr.pdf 사본 생성`에서 `원본 백업 후 교체`로 변경해 OCR 적용 후 회차 상태가 실제로 정상화되도록 조정
  - 2023년 제20회 농산물품질관리사 1차 원본 PDF를 정상 OCR PDF로 교체하고 원본은 `_original_YYYYMMDDHHMMSS.pdf`로 백업
  - 0002 회차가 관리자 catalog/detail에서 `정상`, `published=true`, 선택지 318개, 한글 분리 0개로 표시되는 것을 확인
  - 텍스트 레이어 폰트를 일반 TTF 한글 폰트 우선으로 선택해 `pdftotext` font mismatch 경고 없이 추출되도록 보정

### 주요 내용 + 2026-05-30 08:10:00
- 상세 관리자 진단 지표 화면 압축:
  - PDF 진단 지표 카드를 큰 세로 카드에서 라벨/값/보조설명 기반의 소형 행형 블록으로 변경
  - OCR 사본 경로처럼 긴 텍스트가 카드 높이를 키우지 않도록 말줄임과 툴팁을 적용
  - 상태 박스의 `white-space: pre-wrap` 상속을 진단 결과에서 끊어 HTML 들여쓰기 공백이 화면 여백으로 렌더링되지 않도록 보정
  - 상세 화면이 단일 컬럼으로 전환되는 폭에서도 지표 영역은 다열을 유지하게 해 불필요한 세로 공간을 줄임

### 주요 내용 + 2026-05-30 08:24:00
- OCR PDF 텍스트 레이어 좌표 보정:
  - `scripts/ocr-fallback.mjs`에서 Tesseract `txt`와 함께 `hocr`를 생성하도록 변경
  - `scripts/build-layered-ocr-pdf.py`가 hOCR 줄 bbox를 읽어 원문 이미지의 실제 위치에 invisible 텍스트를 배치하도록 개선
  - hOCR이 한글을 음절 단위 토큰으로 나누는 경우 bbox 간격 기준으로 가까운 음절만 붙이고 실제 단어 간격은 유지
  - 기존처럼 한글 음절 공백과 선택지 변형 토큰은 보정하되, PDF 전체 선택 시 텍스트 선택 영역이 페이지 상단에 재배치되는 문제를 줄이도록 조정

### 주요 내용 + 2026-05-30 08:36:00
- OCR 원본 교체 안전장치 보강:
  - 관리자 OCR `원본 백업 후 교체` 모드는 OCR 결과 PDF를 먼저 진단하고 품질 기준을 통과한 경우에만 원본을 교체
  - 품질 기준을 통과하지 못한 OCR 결과는 원본을 덮지 않고 `_ocr_failed_YYYYMMDDHHMMSS.pdf` 산출물로 분리 보관
  - `_ocr_failed`, `_ocr_rejected` PDF도 파생 파일로 판정해 문제 원본/정답 원본 후보에 섞이지 않도록 처리

### 주요 내용 + 2026-05-30 08:45:00
- 상세 관리자 회차 정보 영역 압축:
  - 회차 정보 요약을 큰 세로 카드 목록에서 라벨/값 기반의 다열 그리드로 변경
  - 문제 파일명, 등록명, 정답 경로처럼 긴 값은 말줄임으로 표시하고 전체 값은 툴팁으로 확인하도록 구성
  - 좁은 화면에서도 회차 정보와 문제/정답 진단이 불필요하게 한 줄씩 커지지 않도록 반응형 기준을 분리

### 주요 내용 + 2026-05-30 08:52:00
- OCR 적용 기본값 복원:
  - 상세 관리자 화면의 `적용 방식` 기본 선택을 `_ocr.pdf 사본 생성`으로 변경
  - 원본 교체는 사용자가 명시적으로 `원본 백업 후 교체`를 선택한 경우에만 실행되도록 UI 기본 흐름을 조정

### 주요 내용 + 2026-05-30 09:03:00
- OCR 재실행 기준 정리:
  - 원본 PDF가 이미 문항번호/선택지 구조와 텍스트 레이어 기준을 통과하면 기본 OCR 재실행을 차단
  - 상세 관리자 화면에서 정상 구조 PDF는 `문항 구조 정상`으로 표시하고, 본문 OCR 텍스트는 보조용이라 원문과 100% 일치하지 않을 수 있음을 안내
  - 강제 OCR을 선택한 경우에만 정상 구조 PDF도 OCR 가능하되 품질 저하 경고를 확인하도록 구성
  - fallback OCR 기본값을 한국어 우선(`kor`, PSM 4)으로 조정하고, `90:`, `20:`, `8)`, `4)` 같은 선택지 변형 토큰 보정을 추가
  - `_ocr_failed`, `_ocr_rejected` PDF가 manifest 생성 시 문제 회차로 등록되지 않도록 공통 제외 패턴을 확장
  - 원본보다 품질이 낮았던 기존 복사본 `_ocr.pdf`는 `_ocr_rejected_YYYYMMDDHHMMSS.pdf`로 분리해 기본 OCR 사본으로 표시되지 않도록 정리

### 주요 내용 + 2026-05-30 08:42:28
- 카테고리 단위 JSON 갱신 추가:
  - 관리자 화면의 전체 `JSON 재생성` 기능은 유지하고, 선택한 기존 카테고리만 `question.json`/`correct.json`을 갱신하는 기능을 별도로 추가
  - `gen_question.py`, `gen_correct.py`에 `--category-path` 옵션을 추가해 기존 `json/category.json`에 등록된 카테고리 범위만 재스캔
  - 부분 갱신 시 기존 `questionNo`는 `questionPdf` 기준으로 유지하고, 새 문제 파일만 전체 최대 번호 다음 값으로 추가
  - 새 카테고리나 디렉토리 구조 변경은 전체 JSON 재생성을 먼저 실행해야 하며, 기존 카테고리에 파일을 추가·수정한 경우에는 카테고리 행의 `JSON 갱신`으로 처리
  - 관리자 API `/api/admin/rebuild-category`를 추가해 선택 카테고리 갱신을 로컬 Node 관리자 서버에서 실행

### 주요 내용 + 2026-05-30 08:49:56
- OCR 실패 산출물 관리 보강:
  - `_ocr.pdf` 같은 OCR 파생 PDF를 진단할 때 NFC/NFD 파일명 차이로 원본 회차의 문항 수가 매칭되지 않는 문제를 수정
  - 품질 기준을 통과하지 못한 OCR 사본은 `_ocr.pdf`로 남기지 않고 `_ocr_failed_YYYYMMDDHHMMSS.pdf` 산출물로 분리
  - 상세 관리자 화면에서 품질 미달 OCR 산출물은 게시/풀이에 사용하지 않는다고 명확히 표시
  - 현재 OCR은 모든 스캔 문서를 완벽하게 복구하는 범용 해법이 아니며, 원본 PDF의 구조/텍스트 품질 진단과 실패 산출물 분리를 우선 기준으로 관리

### 주요 내용 + 2026-05-30 09:25:08
- OCR 품질과 풀이 가능 여부 분리:
  - 게시/풀이 가능 판정을 문제 PDF와 `correct.json`/정답 원본 기준으로 변경하고 OCR 품질 경고는 게시 차단 조건에서 제외
  - `index.html`, `category.html`, 관리자 catalog에서 `ocrPublished`/`ocrStatus` 때문에 회차가 숨겨지지 않도록 조정
  - 관리자 목록과 상세 화면에서 `OCR 품질`을 보조 진단으로 표시하고, OCR 경고가 있어도 원본 PDF와 정답 JSON으로 풀이 가능하다는 안내 추가
  - OCR 품질 미달 산출물은 오류 종료 대신 `_ocr_failed_...pdf`로 보관하고 최종 보관 경로를 로그/화면에 별도 표시
  - `gen_correct.py`는 정답 JSON 생성 성공 시 `published:true`를 유지하고 OCR 결과는 `ocrStatus`, `ocrWarnings` 보조 필드로만 저장
  - `scripts/anchor-ocr.mjs`와 `/api/admin/anchor-ocr`를 추가해 전체 OCR PDF 대신 문항번호 위치만 `*_anchor.json`으로 생성하고 `anchorMap`으로 연결
  - `quiz.html`은 번호 OCR 위치맵을 실시간 PDF 패턴 분석과 캐시보다 우선 적용해 한문제 보기/문항 이동을 안정화
  - 관리자 상세에 `위치맵 확인`을 추가해 감지/보간 문항, 페이지별 매핑, 샘플 좌표를 확인하고 `anchorDebug=1` 풀이 화면에서 PDF 위 기준선을 검증
  - 풀이 화면에서 선택지 앵커가 부족해도 선택 상태 배지와 fallback 동그라미를 표시해 사용자가 선택 여부를 확인 가능하게 보정

### 주요 내용 + 2026-05-30 13:19:48
- 문제·문항 위치맵 명칭 정리:
  - 관리자 상세의 `번호 OCR 생성` 명칭을 `문제·문항 위치맵 생성`으로 변경해 OCR PDF 생성이 아니라 풀이 좌표 JSON 생성 기능임을 명확히 표시
  - 위치맵 JSON에 문제 시작 위치(`questionPageMap`, `questionTopRatioMap`)와 선택지 위치(`choiceAnchorMap`)를 함께 저장하도록 확장
  - 선택지 위치는 렌더된 PDF 이미지의 원형 보기 번호 후보를 우선 반영하고, 이미지 후보가 불안정한 문항은 기존 문제 밴드 기반 추정 좌표로 보완
  - `quiz.html`은 저장된 선택지 1~5 좌표를 PDF 텍스트/OCR 탐지보다 우선 사용하고, 모바일 답 선택 UI도 1~5까지 대응
  - 관리자 `위치맵 확인` 결과에 감지 문제 수, 보간 문항 수, 선택지 위치 수, 선택지 위치 누락 문항을 함께 표시
  - 2023년 제20회 농산물품질관리사 1차 회차 위치맵을 재생성해 문제 100개, 선택지 위치 400개 기준으로 검증

### 주요 내용 + 2026-05-30 14:02:00
- 인쇄 문제번호/정답번호 시작값 분리:
  - 문제 위치맵의 `printedQuestionNoMap`을 풀이 화면 디버그/상태/문항 표시에도 적용해 36번 시작 회차는 `36번`, `36-1`처럼 보이도록 조정
  - 정답표도 1번 시작을 가정하지 않고 `answerStartNo`, `answerEndNo`, `printedAnswerNoMap`을 `correct.json` 항목과 question manifest에 저장
  - `gen_correct.py`/`pdf_manifest_lib.py`가 정답표의 인쇄 번호를 앱 내부 문항 순서로 변환하고, 기존 sparse 정답 배열도 풀이 화면에서 자동 매핑
  - PDF 동적 정답 분석도 16번/36번 같은 정답표 번호를 내부 1번부터의 채점 배열로 변환
  - 선택지 위치맵 생성 시 2열/세로/가로 배치 판정을 보정하고, `anchorDebug=1`에서는 문제번호-선택지번호 붉은 원으로 좌표를 확인

### 주요 내용 + 2026-05-30 14:45:16
- 문제·문항 위치맵 신뢰 좌표 기준 강화:
  - `scripts/anchor-ocr.mjs`에서 선택지 좌표를 기하 추정값으로 채워 넣지 않고, 렌더된 PDF 이미지에서 실제 검출된 좌표만 `choiceAnchorMap`에 저장하도록 변경
  - 예전 위치맵에 남아 있는 `anchor-geometry`, `anchor-image-fill` 좌표도 풀이 화면과 관리자 검증에서 신뢰 좌표로 사용하지 않도록 필터링
  - 짧은 문제 밴드에서 선택지 하단 행이 잘리는 문제를 줄이기 위해 이미지 검색 밴드를 완화하고, 세로형 선택지는 일부 누락 시에도 가로형 오판보다 세로형 실제 좌표를 우선 저장
  - 위치맵 JSON에 `sourceStats`를 추가해 문제 감지 수, 선택지 감지 수, 선택지 커버리지, 누락 문항을 확인 가능하게 구성
  - 2023년 제20회 농산물품질관리사 1차 위치맵을 재생성해 선택지 실제 검출률을 343/400(85.75%)로 확인하고, 남은 누락은 경고로 표시

### 주요 내용 + 2026-05-30 15:07:31
- 문제 시작 위치맵 보간 보정:
  - 새 PDF 페이지 첫 문제번호가 OCR에서 누락된 경우 기존처럼 다음 문제에서 고정값만 빼지 않고 페이지 상단 기준으로 문제 시작선을 보정
  - 문제 시작선이 문제 제목 글자를 가로지르지 않도록 전체 시작 위치를 소폭 위로 이동해 한문제 컷 상단 여백을 확보
  - 2023년 제20회 농산물품질관리사 1차에서 5번, 10번 등 페이지 첫 문항의 점선이 선택지 중간에 찍히던 문제를 확인 후 재생성
  - 재생성 후 선택지 실제 검출률은 360/400(90.0%)로 확인

### 주요 내용 + 2026-05-30 15:32:45
- 위치맵 캐시/한문제 하단 컷 보정:
  - 풀이 화면이 브라우저에 저장된 예전 `pdfNav`를 계속 사용하지 않도록 `PDF_MAP_VERSION`을 갱신하고 anchorMap 생성 시각/좌표 비교 후 최신 위치맵을 다시 적용
  - anchorMap 기반 다음 문항 경계는 신뢰 가능한 경계로 보고, 텍스트 추정 실패용 큰 하단 여백을 적용하지 않도록 한문제 컷 하단 계산을 조정
  - `CROP_CACHE_VERSION`을 갱신해 예전 crop 계산 결과를 무효화
  - 2023년 제20회 농산물품질관리사 1차 5번/10번 한문제 보기에서 하단 선택지 문장이 잘리지 않는 것을 브라우저로 확인

### 주요 내용 + 2026-05-30 16:04:00
- 선택지 가로/세로/2행 위치맵 판정 보정:
  - 원형 선택지 기호가 끊겨 감지되는 스캔 PDF에서도 후보가 잡히도록 이미지 후보 크기/형태 조건을 완화
  - ①~④가 한 줄에 모두 잡히는 경우 지문 박스 내부 후보를 섞은 2행 그리드보다 가로형을 우선 적용
  - 2행 배치가 완전하게 잡히면 다음 문제번호 후보를 섞은 세로형 오판보다 그리드를 우선 적용
  - 완전한 세로 선택지 시퀀스가 있으면 오른쪽 본문 글자를 조합한 가짜 그리드를 버리도록 시작 위치 비교 기준 추가
  - 로컬 PDF 경로는 후보 탐색 전에 즉시 사용하고, 후보 fetch는 타임아웃을 둬 `PDF 경로 확인 중...` 장기 대기를 방지
  - 2023년 제20회 농산물품질관리사 1차 위치맵을 재생성해 5~12번 페이지 구간에서 세로/2행/가로 선택지 좌표를 이미지 오버레이로 확인

### 주요 내용 + 2026-05-30 16:48:00
- 선택지 중심좌표/마킹 표시 보정:
  - 이미지 기반 선택지 위치맵 좌표가 원 중심임을 `anchorMode:center`로 저장하고, 풀이 화면의 빨간 디버그 원/파란 선택 원이 같은 중심좌표를 사용하도록 공통 투영 함수로 통일
  - 선택 표시 원 반지름을 키워 선택지 번호를 직접 덮는 체크 느낌으로 표시되게 조정하고, 기존 PDF/crop 캐시 버전을 갱신
  - 2행 선택지 4개가 모두 잡힌 경우에만 세로형보다 그리드를 우선하고, 왼쪽 세로 후보가 먼저 시작하면 오른쪽 본문 글자를 섞은 가짜 그리드를 버리도록 기준 보정
  - 페이지 마지막 문항은 다음 문제 시작점이 없어 하단 선택지가 잘리지 않도록 선택지 검색 하단을 확장
  - 2023년 제20회 농산물품질관리사 1차 위치맵을 재생성해 선택지 실제 검출률 389/400(97.25%)를 확인하고, 누락 문항은 틀린 좌표 대신 누락 상태로 유지

### 주요 내용 + 2026-05-30 17:36:55
- 문제·문항 위치맵 페이지 시작/선택지 배치 추가 보정:
  - 새 페이지 첫 문제번호 OCR이 누락된 경우 이전 페이지 마지막 문제와 현재 페이지 첫 검출 문제 사이의 실제 상단 여백을 기준으로 시작선을 분배하도록 보정
  - 가로/2행/세로 선택지의 기대 높이를 분리해 문제 제목 줄을 선택지로 오판하거나 선택지를 지나치게 아래쪽에서 찾는 문제를 줄임
  - 다음 문제 제목과 선택지 후보가 한 영역에 섞이는 경우 가짜 2행 그리드를 제외하고 실제 선택지 배치가 먼저 시작되는 후보를 우선 적용
  - 2023년 제20회 농산물품질관리사 1차 위치맵을 재생성해 선택지 실제 검출률 397/400(99.25%)를 확인하고, 22/85/94번의 일부 누락은 틀린 좌표 대신 누락 상태로 유지

### 주요 내용 + 2026-05-30 17:45:00
- 풀이 화면 PDF.js 로딩 안정화:
  - `vendor/pdf.min.js`, `vendor/pdf.worker.min.js`를 추가해 외부 CDN이 지연되거나 차단돼도 풀이 화면이 로컬 웹서버에서 PDF를 렌더링하도록 구성
  - `quiz.html`의 PDF.js 로더를 로컬 파일 우선, CDN fallback 순서로 변경하고 6초 타임아웃을 적용
  - 일부 내장 브라우저에서 PDF.js가 `globalThis` 전역에 붙지 않는 경우를 대비해 로드 직전 전역 shim을 보강
  - PDF.js vendor UMD 래퍼도 `globalThis → window → self` 순서로 전역 객체를 찾도록 보정
  - 로컬 vendor 파일에는 버전 쿼리를 붙여 브라우저가 이전 CDN 실패 상태나 예전 vendor 캐시를 계속 쓰지 않도록 구성
  - 문제/정답 PDF 문서 로딩 시 공통 worker 설정 함수를 사용하도록 정리

### 주요 내용 + 2026-05-30 18:03:00
- 선택지 위치맵 지문 박스 오탐 보정:
  - 렌더 이미지에서 긴 가로선을 감지해 문항 내부 지문/표 박스 하단을 찾고, 선택지 후보는 박스 하단 이후에서만 고르도록 변경
  - 1번처럼 박스 안 본문 글자가 가로형 ①~④로 오판되는 문제를 공통 로직에서 차단
  - 지문/표 박스 뒤 4지선다에서 2행 선택지가 완전하게 감지되면 한 줄 본문 글자 조합보다 2행 그리드를 우선 적용
  - 문제 시작점과 지문 박스 상단선이 가까운 92번 유형도 박스 감지에 포함되도록 상단선 허용 범위 완화
  - 박스가 없는 90/93번 같은 2행 보기에서도 짧은 간격의 완전한 그리드를 가로형 오판보다 우선하도록 보정

### 주요 내용 + 2026-05-30 18:45:52
- 선택지 위치맵 가로형/2행 오탐 추가 보정:
  - 2행 그리드 후보의 왼쪽 열이 실제 선택지 원보다 페이지 가장자리 본문 글자에 붙는 경우 가짜 그리드로 제외
  - 가로형 4번 보기 기준 x좌표를 실제 ④ 원 위치로 당겨 ④ 뒤의 본문 글자를 선택지로 오인하지 않도록 수정
  - 풀이 화면의 위치맵/컷 캐시 버전을 갱신해 이미 저장된 이전 좌표가 계속 보이지 않도록 처리
  - 2023년 제20회 농산물품질관리사 1차 위치맵을 재생성해 1번 지문 박스형, 26번 가로형, 90/92/93번 2행형, 100번 가로형을 오버레이로 확인
  - 재생성 결과 선택지 실제 검출률은 397/400(99.25%)이며, 62/85/94번의 일부 누락은 틀린 좌표 대신 누락 상태로 유지

### 주요 내용 + 2026-05-30 19:33:31
- 선택지 위치맵 세로형/불완전 가로형 오탐 보정:
  - 4지선다 가로형은 ①~④가 모두 잡힌 경우만 인정해 본문 글자나 문제번호를 보기 원으로 잘못 표시하지 않도록 변경
  - 세로형 보기에서 첫 보기만 누락된 경우 ②를 ①로 당겨 붙이지 않도록 시작 번호 추정 기준을 보수적으로 조정
  - 문제번호 바로 아래에 첫 보기가 붙는 문항은 상단 후보 제외 범위를 줄여 85번 ①처럼 실제 선택지가 누락되지 않도록 보정
  - 2023년 제20회 농산물품질관리사 1차 위치맵을 재생성해 5/9/20/21/37/39/50/56/62/85/94/95/96번의 보기 좌표가 선택지 원 기준으로 잡히는 것을 확인
  - 재생성 결과 선택지 실제 검출률은 399/400(99.75%)이며, 남은 76번 일부 누락은 틀린 좌표 대신 누락 상태로 유지

### 주요 내용 + 2026-05-30 19:52:18
- 선택지 위치맵 문제번호 행/완전 가로형 추가 보정:
  - `5.`처럼 좁고 진한 문제번호 숫자가 ① 보기로 오인되는 경우를 후보에서 제외
  - 제목 행에 문제번호가 포함된 경우 해당 행을 가로형 선택지 후보에서 제외
  - ①~④가 한 줄에 모두 잡힌 완전 가로형은 문제번호와 다음 문항을 섞은 불완전 세로 후보보다 우선 적용
  - 2023년 제20회 농산물품질관리사 1차 위치맵을 재생성해 5/76/77번을 포함한 선택지 400/400개가 실제 이미지 좌표로 검출되는 것을 확인
  - 풀이 화면 위치맵/컷 캐시 버전을 갱신해 브라우저가 이전 좌표를 재사용하지 않도록 처리

### 주요 내용 + 2026-06-02 14:18:14
- 회차 목록 UI 재설계:
  - `category.html`을 검색/상태 필터/정렬/요약 지표가 있는 회차 목록 화면으로 개편
  - 하위 카테고리와 개별 회차를 같은 목록에서 구분 표시하고, 회차별 연도/시험유형/문항 수/위치맵 상태를 노출
  - 모바일에서는 회차 행과 액션 버튼이 세로 배치로 전환되도록 반응형 스타일 적용

### 주요 내용 + 2026-06-02 14:24:32
- 서비스 회차 목록 UI 단순화:
  - 사용자 화면에서 `풀이 가능`, `점검 필요`, `하위 목록` 상태 필터와 운영 상태 배지를 제거
  - 회차 목록은 검색/정렬/풀이 시작/문제 PDF/정답 파일 중심으로 정리
  - 하위 카테고리는 서비스 문맥에 맞게 과목 항목으로 표시

### 주요 내용 + 2026-06-02 14:31:28
- 관리자 상세 초기화 UI 재설계:
  - `admin-detail.html`의 위치맵/앵커/회차 초기화 버튼을 `초기화 작업` 패널로 분리
  - 회차 상태, 정답 JSON, 위치맵, 수동 편집 여부를 요약 카드로 표시
  - 특정 문제 위치, 특정 문항 위치, 특정 문항 클릭영역, 전체 문항 클릭영역을 관리자 화면에서 직접 초기화하는 범위별 실행 버튼 추가

### 주요 내용 + 2026-06-02 14:42:39
- 관리자 목록 화면 UI 재설계:
  - `admin.html`의 문제 회차 표를 `회차`, `상태 / 진단`, `문항 / 위치`, `초기화`, `작업` 중심으로 재구성
  - 초기 상태, 위치맵 없음, 수동 편집 상태 필터를 추가하고 문제/카테고리 목록이 같은 상태 판정 기준을 사용하도록 정리
  - 회차별 초기화 버튼을 상세 화면의 `초기화 작업` 패널로 바로 연결해 문제 상태, 문항, 문항 영역 위치 초기화 진입을 단축

### 주요 내용 + 2026-06-02 14:47:35
- 관리자 API 호출 포트 보정:
  - `admin.html`, `admin-detail.html`이 정적 서버 포트에서 열려도 `/api/admin/*` 요청은 `localhost:8787` Node 관리자 서버로 보내도록 보정
  - `scripts/admin-server.mjs`에 로컬 교차 포트 요청용 CORS 헤더와 `OPTIONS` preflight 처리를 추가
  - `anchor-manual-delete`, `question-reset` 요청이 정적 서버에서 `not found`로 떨어지는 문제를 방지

### 주요 내용 + 2026-06-02 14:55:32
- 문제 회차 관리 화면 분리:
  - `admin-questions.html`을 추가해 문제 회차 목록, 상태/진단, 문항/위치 정보를 전용 화면에서 관리하도록 분리
  - `admin.html`은 카테고리, 파일/업로드, JSON/Git 운영 도구 중심으로 축소하고 카테고리별 `회차관리` 링크를 추가
  - 회차 목록의 `초기화` 버튼과 `#resetPanel` 직접 링크를 제거해 초기화는 상세 화면 내부에서만 실행되도록 정리

### 주요 내용 + 2026-06-02 19:54:33
- 위치맵 생성 진행률 표시:
  - `scripts/admin-server.mjs`에 위치맵 생성 백그라운드 작업 API(`/api/admin/anchor-ocr/start`, `/status`, `/jobs`, `/cancel`) 추가
  - `anchor-ocr.mjs` 실행 로그의 `ANCHOR page x/n` 값을 서버 작업 상태로 파싱해 실제 처리 페이지 기준 진행률을 계산
  - `admin-detail.html`의 위치맵 생성 다이얼로그에서 진행률, 경과 시간, 페이지 처리 현황, 최근 로그, 취소 버튼을 표시하도록 변경

### 주요 내용 + 2026-06-02 15:05:09
- 문제 풀이 전체보기 응답 없음 완화:
  - 전체보기 PDF 렌더링 전용 출력 배율을 한문제 보기보다 낮게 분리해 대량 캔버스 픽셀/메모리 사용량을 줄임
  - 전체보기 페이지 렌더링에서 고비용 여백 스캔을 생략하고, 페이지 렌더 루프 중간에 프레임 양보를 추가
  - 전체보기 마커 재그리기를 토큰 기반으로 취소 가능하게 만들고, 반복 요청은 requestAnimationFrame으로 병합
  - 위치 편집 저장 후 전체보기 마커 갱신을 기다리지 않고 예약 처리해 저장 완료 UI가 오래 묶이지 않도록 조정

### 주요 내용 + 2026-06-02 16:12:19
- 위치편집 앵커 상세 로딩 분할:
  - `/api/admin/anchor-data` API를 추가해 `_anchor.json` 기본 위치맵은 요약으로, `questionSegments`/`choiceAnchorMap`/`choiceClickAreaMap`은 요청 문항만 부분 반환
  - `quiz.html` 초기 위치맵 로드를 요약 응답 우선으로 변경하고, 현재 문항·보이는 전체보기 페이지·클릭 대상 문항만 상세 앵커를 지연 로드
  - 전체보기 마커 갱신을 보이는 페이지/포커스 주변 문항 중심으로 제한하고, PDF 렌더 완료 후 앵커 표시 레이어는 비동기로 갱신하도록 변경
  - `localStorage` UI 저장값에서 문항영역/선택지 상세 맵을 제외해 다음 진입 시 상세 앵커를 통째로 복원하지 않도록 조정
  - manifest의 `anchorMap`이 비어 있을 때 문제 PDF 경로에서 `_anchor.json` 후보를 추론하도록 보강

### 주요 내용 + 2026-06-02 19:41:16
- 문제풀이 편집 오버레이 분리:
  - `위치디버깅`/`위치 편집` 명칭을 `앵커영역`/`앵커편집`으로 변경하고 상단에 `문항편집` 진입 버튼을 추가
  - 앵커편집과 문항편집을 패널 내부 스위치로 전환하도록 분리하고, 문항편집은 빨간 문항영역 전용 오버레이와 컨트롤만 표시
  - 편집 패널 도킹 위치에 `위`/`아래`를 추가해 왼쪽/위/아래/오른쪽 네 방향으로 붙일 수 있도록 구현

### 주요 내용 + 2026-06-02 19:57:29
- 짧은 문항 선택지 앵커 보정:
  - 저장된 보기 앵커가 없는 아주 짧은 전폭 문항은 새 문제를 만들지 않고 기존 `questionSegments` 안에서만 ①~④ 한 줄 보기 앵커를 제한적으로 추정
  - 풀이 화면의 앵커 표시, 문항영역 표시, 편집 히트 테스트가 해당 fallback 앵커를 사용하도록 연결
  - `scripts/anchor-ocr.mjs` 재생성 로직에도 동일한 짧은 문항 복구 조건을 추가해 2023년 3번 같은 문항의 보기 좌표를 복원

### 주요 내용 + 2026-06-02 20:03:54
- GitHub Pages PDF 경로 정규화:
  - 서버에는 NFC 한글 파일명으로 배포되어 NFD 자모 URL이 404가 나는 것을 확인하고, 초기 `question`/`answer`/`correct` 쿼리를 PDF 로딩 전에 NFC로 정규화
  - 정적 배포 환경에서는 `pdf/...` 상대경로를 HEAD 후보 탐색에 오래 묶지 않고 NFC URL로 즉시 사용하도록 보정
  - 로컬 관리자 환경은 기존처럼 로컬 파일 경로와 후보 확인 로직을 유지

### 주요 내용 + 2026-06-03 00:04:53
- 카테고리 목록 더블클릭 이동:
  - `admin.html` 카테고리 목록 행에 더블클릭 이동을 추가해 해당 카테고리의 `admin-questions.html` 회차관리 화면으로 바로 진입
  - 행 내부 `보기`, `회차관리`, `JSON생성` 버튼/링크 더블클릭은 기존 동작을 방해하지 않도록 제외


## 공통
- 중요한 내용으로 판단한 경우, AGENTS.md에 내용 추가
- 코드를 수정,추가한 경우 한줄 변경은 코드 끝에 한줄 주석으로 수정한 내용 (설명 + 날짜)과 함께 표시 (prefix : SOFTM-"키워드"), 여러줄일 경우 블럭주석으로 시작과 끝영역을 표시하고 주석처리한다.
- 설명은 한글로 해줘
- "커밋해줘","커밋" 명령시 : Git Commit message Rule : "codex_" + versionCode + "_" + "수정내용" + "_날짜시분초"

## 문항 활성화 표시 : 문항 선택
  - 오답 : 스트라이크 붉은선
  - 정답 : 언더라인 파란선
- 