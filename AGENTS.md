# AGENTS.md

## 공통 작업 규칙
- 중요한 내용으로 판단한 경우 이 파일에 기록한다.
- 코드 수정/추가 시 변경 주석을 남긴다.
- 한 줄 변경은 해당 코드 끝에 `SOFTM-키워드: 설명 - yyyy-MM-dd` 형식 한 줄 주석을 남긴다.
- 여러 줄 변경은 `SOFTM-키워드 시작/끝` 블럭 주석으로 범위를 표시하고 설명과 날짜를 포함한다.
- 사용자 설명과 최종 보고는 한글로 작성한다.
- "커밋해줘" 또는 "커밋" 요청 시 커밋 메시지는 `codex_` + `versionCode` + `_` + `수정내용` + `_` + `날짜시분초` 형식으로 작성한다.
- `versionCode`가 저장소에서 바로 확인되지 않으면 커밋 전에 확인한다.

## 문항영역/활성화 표시 기준
- `choiceClickAreaMap`은 클릭 판정용 문항영역이다.
- 문항 활성화 표시는 시각 피드백용 별도 geometry로 계산한다.
- 문항 앵커가 신뢰 가능한 경우 활성화 표시는 문항 앵커 원을 기준으로 한다.
- 2단 전체보기 스크롤 문제 선택은 `questionSegments`/`questionColumnBoundsMap`/문제 앵커 기준으로 단을 판단하고, 문항 앵커는 보조 fallback으로만 사용한다.
- 오답 표시는 스트라이크 붉은선, 정답 표시는 언더라인 파란선으로 부른다.
- 전체보기 문항영역은 한문제보기의 crop 기준 품질을 목표로 하되, 기존 anchor JSON/correct.json 데이터는 자동 보정하지 않는다.
- 문항영역 초기 계산은 문항 앵커 옆 텍스트 픽셀 탐색을 우선하고, 문항 앵커 원 기준 Y 범위와 다음 문항/단 경계를 넘어가지 않는 제한을 함께 적용한다.
- 저장 앵커맵이 없는 PDF/OCR 추정 문항 앵커는 텍스트 픽셀 fit을 강제하지 않고, 행/다음 앵커 경계 기반 문항영역을 우선 사용한다.
- `segment-*`, `pdf-*`, `ocr-*`, `inferred:true` 문항 앵커는 편집 가능한 저장 앵커로 취급하지 않는다.
- 선택편집으로 문항 앵커 위치를 다시 찍을 때는 같은 문제의 기존/형제 문항 앵커 원 크기를 우선 보존하고, 없을 때만 작은 기본 크기를 사용한다.
- 세로형 문항영역은 단일 행의 긴 선택지 텍스트가 잘리지 않도록 단/문제 경계까지 스캔한 뒤 실제 텍스트 픽셀 끝으로 줄인다.
- 문항 활성화 선은 문항 앵커 옆에서 시작해 문항 텍스트 끝까지 표시하며, 전체보기/한문제보기 모두 CSS px 기준으로 비슷한 굵기를 유지한다.
- 일반 풀이 모드에서 문항 앵커 원 클릭과 문항영역 클릭은 같은 선택 커밋 함수로 처리한다.

## 관리자 위치맵 상태 기준
- 관리자 목록/상세의 위치맵 상태는 manifest의 `anchorMap` 문자열만 믿지 않고 실제 `_anchor.json` 파일 존재를 기준으로 판단한다.
- manifest에 위치맵 보조 필드가 남아 있어도 실제 파일이 없으면 위치맵 삭제 상태와 동일하게 표시한다.
- quiz 화면에서 manifest row를 찾을 때 `questionNo`만 믿지 않고 현재 문제 PDF 경로를 우선 비교한다.

## PDF 렌더 품질 기준
- 전체보기 PDF 품질은 한문제보기와 같은 `outputScale` 기준을 사용한다.
- 전체보기 렌더 속도 최적화가 필요해도 PDF 캔버스 자체를 저화질 배율로 낮추지 않고, 앵커/문항영역 데이터 로딩 최적화와 분리해서 처리한다.
