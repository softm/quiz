# 요청 프롬프트 해결 요소 정리

이 문서는 이번 스레드에서 나온 여러 요청을 개별 질문이 아니라 실제로 해결해야 할 작업 요소 기준으로 묶은 정리입니다.

## 1. 앵커맵 용어와 개념 통일

위치맵이라는 표현은 앵커맵으로 통일한다.

앵커맵은 문제 앵커와 문항 앵커를 포함한다. 문항영역 맵은 앵커맵과 구분되는 별도 영역 보정 데이터로 본다.

관리자 화면과 풀이 화면에서 같은 용어를 사용해야 한다.

- 위치맵 → 앵커맵
- 문제 앵커
- 문항 앵커
- 문항영역 맵
- 앵커맵 초기화
- 문항영역 맵 초기화

## 2. 앵커 편집과 문항영역 안정화

문제 앵커, 문항 앵커, 문항영역 맵은 따로 보이지만 실제로는 하나의 편집 흐름에서 같이 안정화해야 한다.

핵심 원칙은 앵커 수정이 클릭한 PDF 위치의 문제 범위를 벗어나지 않아야 한다는 것이다. 전체보기, 2단 문제지, 스크롤락 상태에서도 이 원칙은 유지한다.

회귀 테스트 기준 회차는 다음으로 고정한다.

- 2단 문제지: `[2014.1학기 기말시험] 모바일앱프로그래밍`
- 1단 문제지: `2023년 제20회 농산물품질관리사 1차`

### 문제 앵커 저장 대상 검증

문제 앵커는 해당 문제가 매핑된 PDF 페이지 안에서만 수정되어야 한다.

특히 1번 문제를 선택한 상태에서 두 번째 페이지를 클릭해 1번 문제 앵커가 이동되는 동작은 막아야 한다.

필요한 방어 조건은 다음과 같다.

- 클릭한 PDF 페이지와 선택 문제의 `questionPageMap` 페이지가 다르면 저장하지 않는다.
- 문제 앵커의 기존 페이지 정보가 있으면 그 페이지와도 비교한다.
- 2단 문제지에서는 클릭한 단과 문제의 기존 단이 다르면 저장하지 않는다.
- 스크롤락은 자동 포커스 변경만 막고, 다른 페이지 저장을 허용하는 기능이 아니다.

### 문항 앵커 편집 경로 정리

문항 앵커도 문제 앵커와 같은 페이지/단 검증을 받아야 한다.

전체보기에서 문항 앵커가 조정되지 않거나 2단 문제지에서 잡히지 않는 문제는 전체보기용 hit-test 경로를 사용하도록 정리한다.

문항 앵커 UI는 PDF 본문을 가리지 않도록 축소한다.

- 문항 앵커 원 크기 축소
- 문항 앵커 라벨 축소
- 전체보기와 단일보기의 클릭/드래그 경로 분리
- 문항영역 맵에서 앵커맵으로 돌아오면 선택편집으로 복귀

### 문항영역 맵 표시와 저장 기준

문항영역 맵은 저장된 데이터가 없어도 화면에 임시 표시될 수 있다.

이 경우 저장된 문항영역 맵이 아니라 문항 앵커와 문제 앵커를 기준으로 계산한 자동 표시로 구분해야 한다. 사용자가 직접 편집한 문항영역만 저장한다.

정리할 기준은 다음과 같다.

- 저장된 문항영역 맵이 있으면 저장값을 우선 표시한다.
- 저장값이 없으면 문항 앵커 기준으로 임시 문항영역을 표시한다.
- 편집한 문항영역만 `choiceClickAreaMap`에 저장한다.
- 문항영역 높이는 문항 앵커 원과 다음 문항/문제 경계 기준으로 계산한다.

## 3. 앵커맵과 문항영역 초기화 의미 정리

초기화는 삭제가 아니라 기준 상태로 되돌리는 동작이다.

초기화 범위는 명확히 구분해야 한다.

- 문제 앵커 초기화: 선택 문제의 문제 앵커 수정값 복원
- 문항 앵커 초기화: 선택 문항 앵커 수정값 복원
- 앵커맵 전체 초기화: 문제 앵커와 문항 앵커 수정값 전체 복원
- 문항영역 맵 초기화: 문항영역 보정값 복원
- 문항영역 맵 전체 초기화: 모든 문항영역 보정값 복원

## 4. 정답 파일 연결 상태 정의

목록의 “정답 파일 연결”은 실제 correct.json이 풀이 화면에서 사용할 수 있는 상태인지 기준으로 판단해야 한다.

단순히 정답 HWP/PDF 경로가 있는 것과 correct.json이 생성되어 연결된 것은 구분해야 한다.

정상 조건은 다음과 같다.

- 원본 정답 파일 경로가 있다.
- 해당 문제 회차와 매칭되는 correct.json이 있다.
- correct.json 안의 문제 범위와 현재 회차의 문항 범위가 맞다.
- 풀이 화면에서 correct 파라미터로 접근 가능하다.
- 풀이 화면에서 정답 표시가 실제로 된다.

## 5. correct.json 생성과 재생성 흐름

문제 상태 초기화 후에도 correct.json을 다시 생성할 수 있어야 한다.

처음 correct.json을 만들 때는 정답 파일을 찾는 기준이 필요하다.

생성 흐름은 다음 순서로 정리한다.

1. 문제 회차의 과목, 연도, 학기, 시험 종류, 문항 범위를 확인한다.
2. 정답 파일 후보를 찾는다.
3. 후보 정답 파일에서 해당 과목과 문항 범위를 추출한다.
4. correct.json을 생성한다.
5. 생성된 correct.json을 문제 회차에 연결한다.
6. 상세 화면과 풀이 화면에서 같은 연결 상태를 보여준다.

관리자 화면에서 API가 404가 나면 서버 실행 상태와 엔드포인트 구현 여부를 먼저 확인한다.

## 6. 게시 가능 조건 정리

게시 가능 여부는 정답 연결과 PDF 진단 상태를 기준으로 판단한다.

기본 정책은 다음과 같다.

- 정답 파일이 연결되지 않으면 게시 불가
- 정답 연결이 없으면 노란색 “정답연결” 칩 표시
- PDF OCR 오류가 있으면 앵커맵이 있어야 게시 가능
- OCR 정상이고 정답 연결이 있으면 게시 가능
- 앵커맵이 필요한 회차인데 앵커맵이 없으면 게시 불가

## 7. 풀이 화면 정답 표시 검증

correct.json이 생성되었다는 것과 풀이 화면에서 정답이 표시되는 것은 별도로 검증해야 한다.

검증할 항목은 다음과 같다.

- correct.json 파일 존재
- correct.json 경로 연결
- 문제 번호 범위 일치
- 풀이 URL의 correct 파라미터 확인
- 답안지에서 정답 표시 확인
- 자동채점 시 정답/오답 상태 표시 확인

## 8. 채점 표시 용어 정리

풀이 화면의 채점 표시 용어를 통일한다.

추천 용어는 다음과 같다.

- 정답 표시: 정답 밑줄
- 오답 표시: 오답 취소선
- 선택 상태: 선택 표시
- 채점 결과: 정오 표시

UI나 코드에서 언더라인, 스트라이크 같은 표현을 그대로 노출하지 않고 사용자 기준의 용어를 사용한다.

## 9. 회귀 방지 체크리스트

앵커 편집을 수정한 뒤에는 같은 문제가 반복되지 않도록 고정 체크리스트를 수행한다.

필수 확인 항목은 다음과 같다.

- `[2014.1학기 기말시험] 모바일앱프로그래밍` 전체보기에서 2단 문제지 기준 확인
- `2023년 제20회 농산물품질관리사 1차` 전체보기에서 1단 문제지 기준 확인
- 전체보기에서 문제 앵커 클릭 이동
- 전체보기에서 문제 앵커 드래그 이동
- 전체보기에서 문항 앵커 클릭 이동
- 전체보기에서 문항 앵커 드래그 이동
- 2단 문제지에서 왼쪽 단 문제 앵커 수정
- 2단 문제지에서 오른쪽 단 문제 앵커 수정
- 스크롤락 상태에서 같은 페이지 클릭 허용
- 스크롤락 상태에서 다른 페이지 클릭 차단
- 문항영역 맵 편집 진입
- 문항영역 맵에서 앵커맵 복귀
- 복귀 후 선택편집 상태 유지
- correct.json 연결 후 풀이 화면 정답 표시

## 10. 문제맵 앵커 편집 버그 요청 템플릿

문제맵 또는 문제 앵커 편집 문제가 다시 발생하면 증상만 말하지 말고 재현 조건, 기대 동작, 수정 범위, 검증 기준을 함께 요청한다.

다음 템플릿을 사용한다.

```md
문제맵/문제 앵커 편집 버그를 실제 재현해서 원인 찾고 수정해줘.

대상 회차:
- 2단 기준: [2014.1학기 기말시험] 모바일앱프로그래밍
- 1단 기준: 2023년 제20회 농산물품질관리사 1차

화면과 모드:
- 화면: quiz.html 전체보기
- 모드: 앵커맵 편집 / 문제 앵커
- 조건: 2단 문제지 또는 1단 문제지
- 스크롤락: ON 또는 OFF
- 선택 문제: 예) 36번(앱 1번)

재현 단계:
1. 전체보기로 연다.
2. 앵커맵 편집을 켠다.
3. 문제 앵커를 선택한다.
4. 문제를 선택한다.
5. 특정 페이지/단/위치를 클릭하거나 드래그한다.

현재 동작:
- 예) 1번 문제 앵커가 해당 페이지에서는 안 움직이고, 2페이지 클릭 시 이동된다.

기대 동작:
- 예) 1번 문제 앵커는 1페이지 왼쪽 단에서만 수정되어야 한다.
- 예) 다른 페이지나 다른 단 클릭은 저장되지 않아야 한다.

수정 범위:
- quiz.html만 수정한다.
- anchor JSON, correct.json, 서버 코드는 건드리지 않는다.
- 실제 저장 클릭으로 데이터가 바뀔 수 있으면 먼저 무저장 검증이나 복사본으로 확인한다.

검증 기준:
- git diff --check -- quiz.html
- inline script syntax check
- 브라우저에서 문제 앵커 / 문항 앵커 / 문항영역 맵 복귀 확인
- 2단 기준 회차와 1단 기준 회차 모두 확인
- 데이터 변경 없이 차단/허용 케이스 확인
```

필수로 포함할 정보는 다음 세 가지다.

- 선택한 문제: 예) `36번(앱 1번)`
- 클릭/드래그 위치: 예) `1페이지 왼쪽 단`, `2페이지 오른쪽 단`
- 잘못된 동작: 예) `저장 안 됨`, `다른 페이지로 이동됨`, `문항 앵커가 대신 잡힘`

복붙용 단일 프롬프트는 다음을 사용한다.

```md
문제맵/문제 앵커 편집 버그를 실제 브라우저에서 재현하고 원인을 찾아서 수정해줘.

대상 회차는 두 개다.
- 2단 문제지: [2014.1학기 기말시험] 모바일앱프로그래밍
- 1단 문제지: 2023년 제20회 농산물품질관리사 1차

확인할 화면과 모드는 다음과 같다.
- 화면: quiz.html 전체보기
- 모드: 앵커맵 편집
- 대상: 문제 앵커와 문항 앵커
- 조건: 스크롤락 ON/OFF 모두 확인

현재 문제는 문제 앵커가 해당 문제의 PDF 페이지/단 안에서만 수정되어야 하는데, 특정 조건에서 수정이 안 되거나 다른 페이지/다른 단 클릭으로 잘못 저장되는 것이다.

특히 다음 케이스를 반드시 확인해줘.
- 2단 문제지에서 36번(앱 1번) 문제 앵커가 1페이지 왼쪽 단에서 수정되는지
- 2단 문제지에서 36번(앱 1번)을 선택한 상태로 2페이지를 클릭했을 때 저장이 차단되는지
- 2단 문제지에서 같은 페이지라도 반대 단 클릭이 저장되지 않는지
- 1단 문제지인 2023년 제20회 농산물품질관리사 1차에서 정상적인 문제 앵커 수정이 막히지 않는지
- 문항 앵커도 문제 앵커와 같은 페이지/단 검증을 받는지
- 문항영역 맵에서 앵커맵으로 돌아왔을 때 선택편집 상태가 유지되는지

수정 범위는 quiz.html로 제한해줘.
- anchor JSON은 수정하지 마.
- correct.json은 수정하지 마.
- 서버 코드는 수정하지 마.
- PDF 렌더링 로직은 이 버그에 직접 필요하지 않으면 건드리지 마.

실제 저장 클릭으로 데이터가 바뀔 수 있으면 먼저 무저장 검증이나 복사본 기준으로 재현해줘.

수정 후 검증은 다음을 포함해줘.
- git diff --check -- quiz.html
- inline script syntax check
- 브라우저에서 문제 앵커 / 문항 앵커 / 문항영역 맵 복귀 확인
- 2단 기준 회차와 1단 기준 회차 모두 확인
- 허용되어야 하는 클릭은 허용되고, 다른 페이지/다른 단 클릭은 저장 차단되는지 확인

최종 답변에는 원인, 수정 파일, 검증 결과, 실제 저장 데이터 변경 여부를 짧게 정리해줘.
```

## 현재 우선순위

가장 먼저 안정화해야 할 순서는 다음과 같다.

1. 전체보기 앵커 저장 대상 검증
2. 2단 문제지 문제/문항 앵커 편집
3. 문항영역 맵 자동 표시와 저장 구분
4. 정답 연결 상태와 correct.json 생성 흐름
5. 게시 가능 조건 정리
6. 회귀 방지 체크리스트와 버그 요청 템플릿 문서화
